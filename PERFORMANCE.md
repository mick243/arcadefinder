# 조회 성능 개선 기록

2026-08-25. k6 부하테스트에서 지목된 병목을 따라 조회 경로를 손봤습니다.
바꾼 것은 넷이고, 전부 **동작은 그대로 두고 DB 를 덜 치게** 만드는 변경입니다.

| # | 무엇 | 파일 |
|---|---|---|
| 1 | 커넥션 풀 상한을 환경변수로 | `lib/db.ts` |
| 2 | 참조 데이터 캐싱 | `lib/cache.ts` (신규) · `lib/arcades.ts` · `lib/tier.ts` |
| 3 | `listPosts` 목록·총계 분리 + 총계 캐시 | `lib/board.ts` · `lib/cache.ts` |
| 4 | 인기글 정렬·검색 인덱스 | `db/migrate-042-posts-scale-indexes.sql` |

---

## 배경 — 왜 이걸 골랐나

부하테스트에서 병목이 **node-postgres 커넥션 풀 상한**으로 확인됐습니다.
근거는 `pg_stat_activity` 였습니다. 부하 중 커넥션이 **정확히 상한에 붙어** 있었고
(10 으로 두면 10개, 50 으로 올리면 53개), 상한이 곧 실측치라는 건 풀이 항상 꽉 차
있다는 뜻입니다.

반대로 병목이 **아닌** 것들도 측정으로 배제했습니다. 다시 파지 마세요.

- **느린 쿼리 아님** — 워밍업 후 전 엔드포인트 5~25ms. `posts` 는 30행짜리 테이블입니다.
- **반경 검색 아님** — haversine 전체 스캔이지만 13ms 로 오히려 빠른 축입니다.
  `lib/arcades.ts` 주석의 PostGIS 이관은 지금 필요 없습니다.
- **Node CPU 아님** — 20 VU 에서 코어 1개의 27%, 200 VU 에서도 56%.
- **Postgres 용량 아님** — 커넥션이 죄다 idle 로 잡혔고 `max_connections` 는 100입니다.

풀에 여유를 주는 길은 둘입니다. **상한을 올리거나(1), 요청당 쓰는 슬롯을 줄이거나(2·3).**
둘 다 했고, 뒤에 글이 5만 건으로 늘었을 때를 대비해 인덱스(4)를 더했습니다.

---

## 1. 커넥션 풀 상한을 환경변수로

`new pg.Pool({ connectionString })` 에 `max` 가 없어 node-postgres 기본값 **10** 으로
돌고 있었습니다. 200 VU × 배치 6 = 최대 1,200 요청이 커넥션 10개를 놓고 줄을 섭니다.

```diff
  async function createPgDb(connectionString: string): Promise<Db> {
    const { default: pg } = await import('pg');
-   const pool = new pg.Pool({ connectionString });
+   const max = Number(process.env.PG_POOL_MAX) || 30;
+   const pool = new pg.Pool({ connectionString, max });
```

**기본값은 30 입니다.** 측정으로 고른 값이고(아래 "풀 상한을 얼마로 할까"), 환경변수로
덮어쓸 수 있습니다.

```bash
PG_POOL_MAX=50 npm run start
```

환경변수 없이 띄운 서버에서 부하 중 커넥션이 33개(상한 30 + 관측용 3)로 잡히는 것을
확인했습니다.

> `max_connections = 100` 이 하드 리밋입니다. 앱 인스턴스가 여럿이면 합산이므로
> 단일 인스턴스는 80~90 이 실질 상한입니다. 코어 수를 한참 넘겨 늘리면 Postgres
> 안에서 컨텍스트 스위칭으로 오히려 느려집니다.

---

## 2. 참조 데이터 캐싱

### 대상

`machines` · `songs` · `charts` 는 **앱 런타임에서 쓰는 경로가 없습니다.**
`scripts/` 의 적재 도구로만 채우는 사실상의 시드 데이터인데, 조회 라우트가 전부
`force-dynamic` 이라 요청마다 DB 를 한 번씩 더 쳤습니다.

세 함수를 캐시했습니다.

| 함수 | 파일 | 쓰는 곳 |
|---|---|---|
| `listMachines()` | `lib/arcades.ts` | `/api/machines`, `/api/chat`, `lib/chat-tools.ts` |
| `listGames()` | `lib/tier.ts` | `/api/tier`, `/api/games`, `getGame()` |
| `listLevels(machineId)` | `lib/tier.ts` | `/api/tier` |

### 왜 라우트가 아니라 데이터 계층인가

**`/api/tier` 를 라우트째 캐시하면 남의 기록이 새어 나갑니다.** 이 응답은 한 몸에
참조 데이터(게임·레벨 목록)와 사용자별 데이터(`playerId` 가 걸린 서열표 · `myVote` ·
`myClear`)를 함께 싣습니다. 먼저 요청한 사람의 투표가 다음 사람에게 그대로 나갑니다.

그래서 캐시는 **참조 데이터를 읽는 함수에만** 겁니다. `getTierBoard()` 는 손대지
않았습니다.

### 왜 Next 의 `unstable_cache` 를 쓰지 않았나

처음엔 `unstable_cache` 로 짰는데 테스트 6건이 깨졌습니다.

```
Invariant: incrementalCache missing in unstable_cache async function listMachinesUncached()
  at cachedCb (next/src/server/web/spec-extension/unstable-cache.ts:113:13)
```

`unstable_cache` 는 **요청 컨텍스트에 실린 `incrementalCache` 가 있어야** 동작합니다.
라우트 핸들러를 직접 호출하는 테스트와 `scripts/` 의 도구는 전부 그 컨텍스트 밖입니다.
서버가 단일 프로세스인 것도 확인했으므로(부하 중 CPU · 커넥션 관측) 프로세스 안에
두는 편이 단순하고 어디서나 똑같이 동작합니다.

### `lib/cache.ts` — 핵심

```ts
export function cacheReference<A extends unknown[], R>(
  fn: (...args: A) => Promise<R>,
  name: string,
  ttlMs: number = TTL_MS,          // 기본 5분
): (...args: A) => Promise<R> {
  return (...args: A): Promise<R> => {
    const key = args.length ? `${name}:${JSON.stringify(args)}` : name;

    const hit = store.get(key);
    if (hit && hit.expiresAt > Date.now()) return hit.value as Promise<R>;

    const value = fn(...args);                       // ← 값이 아니라 Promise 를 담는다
    store.set(key, { value, expiresAt: Date.now() + ttlMs });

    value.catch(() => {                              // 실패는 캐시에 남기지 않는다
      if (store.get(key)?.value === value) store.delete(key);
    });

    return value;
  };
}
```

**결과값이 아니라 진행 중인 Promise 를 담는 것이 핵심입니다.** 캐시가 빈 상태에서
요청 100건이 동시에 들어와도 DB 로 나가는 쿼리는 하나고 나머지는 같은 약속을
기다립니다. 값만 담으면 그 100건이 전부 각자 쿼리를 날려서, 정작 부하가 몰리는
순간에 캐시가 없는 것과 같아집니다.

실패한 조회를 캐시에 남기면 TTL 동안 같은 오류만 돌려주므로 `catch` 로 지웁니다.
그 사이 다른 호출이 항목을 갈아끼웠을 수 있어 **내가 넣은 것일 때만** 지웁니다.

캐시 저장소는 `lib/db.ts` 와 같은 이유로 `globalThis` 에 둡니다 — dev 서버 HMR 마다
새로 뜨면 안 됩니다.

### 적용

```diff
- export async function listMachines(): Promise<Machine[]> {
+ async function listMachinesUncached(): Promise<Machine[]> {
    ...
  }
+
+ export const listMachines = cacheReference(listMachinesUncached, 'machines');
```

`listLevels` 만 한 겹 더 있습니다. **기본 인자를 캐시 바깥에서 채웁니다.**

```ts
const listLevelsCached = cacheReference(listLevelsUncached, 'tier-levels');

export function listLevels(machineId = DEFAULT_MACHINE_ID) {
  return listLevelsCached(machineId);   // 항상 명시적으로 넘긴다
}
```

인자가 캐시 키에 들어가므로, 기본값을 안쪽에 두면 `listLevels()` 와
`listLevels(1)` 이 **같은 데이터를 서로 다른 키로 두 벌** 쌓습니다.

### TTL 과 갱신

기본 5분입니다. 적재 스크립트는 Next 바깥에서 DB 를 직접 고치므로 캐시를 비울 방법이
없습니다 — **실질적인 갱신 수단은 TTL 뿐입니다.** 스크립트로 기종을 추가했다면 최대
5분 뒤에 화면에 반영됩니다. 즉시 반영이 필요하면 서버를 재시작하거나
`clearReferenceCache()` 를 부르세요.

앱 안에서 참조 데이터를 고치는 경로가 생기면 그때 그 쓰기 직후에
`clearReferenceCache()` 를 걸어야 합니다.

---

## 3. `listPosts` — 목록과 총계를 나누고, 총계는 캐시

### 무엇이 문제였나

한 요청에 쿼리 셋을 `Promise.all` 로 동시에 날렸습니다 — **요청당 풀 슬롯 3개**입니다.

1. 목록 (`POST_SELECT` + WHERE + ORDER + LIMIT/OFFSET)
2. `COUNT(*)` 전체 (같은 WHERE)
3. 공지 목록

1번과 2번은 **WHERE 가 완전히 같습니다.** 원래 주석도 그걸 알고 있었습니다.

> 두 쿼리의 WHERE 는 같은 조건이지만 파라미터 번호가 달라 따로 씁니다.
> 한쪽 문자열을 재활용하려면 번호를 치환해야 하는데, SQL 을 정규식으로 고치는
> 코드는 조건이 하나 늘어나는 순간 조용히 틀립니다.

### 1차 시도 — 창 함수로 합쳤다가 되돌렸습니다

`COUNT(*) OVER ()` 로 목록과 총계를 한 쿼리에 합쳤습니다. **글 30건 기준으로는
문제가 없었고 풀 슬롯도 하나 줄었습니다.** 그런데 임시 테이블에 글 5만 건을 넣고
`EXPLAIN ANALYZE` 로 재 보니 뒤집혔습니다.

| 방식 | 5만 건 | 읽은 행 |
|---|---|---|
| 목록(LIMIT 안쪽) + 별도 COUNT | **5.9ms** | 21 + 41,667 |
| `COUNT(*) OVER ()` 한 쿼리 | **141.4ms** | 41,667 |

**24배 느립니다.** 이유는 둘입니다.

- **창 함수가 LIMIT 을 아래로 못 밀어넣습니다.** 20건만 필요한데도 조건에 맞는 행을
  전부 정렬해야 해서, 목록 쿼리가 Index Scan(21행에서 멈춤)에서 **Seq Scan + 전체 정렬**로
  내려앉습니다.
- **그 전부가 4개 조인을 통과합니다.** `machines`·`board_categories`·`players`·`post_likes`
  를 41,667행이 모두 거칩니다. 조인 없이 `posts` 만 훑던 예전 COUNT 는 Index Only Scan
  이라 훨씬 쌌습니다.

당시 "읽는 양은 늘지 않는다" 고 적었는데 **틀렸습니다.** 읽는 행 수는 같지만
**무엇을 통과해서 읽느냐** 가 다릅니다. 30행에서는 둘 다 순식간이라 이 차이가
보이지 않았습니다.

### 최종 — 나누고, 총계는 캐시

목록은 LIMIT 이 인덱스까지 내려가도록 되돌리고, 총계는 캐시로 받습니다.

```diff
- const [{ rows }, notices] = await Promise.all([
-   db.query(
-     `SELECT x.*, (COUNT(*) OVER ())::int AS total_count
-      FROM ( ${POST_SELECT} WHERE ... ) x
-      ORDER BY ${ORDER[sort]}
-      LIMIT $4::int OFFSET $5::int`, ...),
-   pinNotices ? listNotices(playerId) : Promise.resolve([]),
- ]);
+ const [{ rows }, total, notices] = await Promise.all([
+   db.query(
+     `${POST_SELECT}
+      WHERE ...
+      ORDER BY ${ORDER[sort]}
+      LIMIT $4::int OFFSET $5::int`, ...),
+   countPosts(tab, category, term, minLikes, excluded),
+   pinNotices ? listNotices(playerId) : Promise.resolve([]),
+ ]);
```

정렬 키도 테이블 별칭을 되살렸습니다 — 정렬이 다시 안쪽 쿼리로 들어갔기 때문입니다.

```diff
  const ORDER: Record<PostSort, string> = {
-   recent: 'created_at DESC, id DESC',
+   recent: 'p.created_at DESC, p.id DESC',
-   popular: 'like_count DESC, comment_count DESC, created_at DESC',
+   popular: 'p.like_count DESC, p.comment_count DESC, p.created_at DESC',
  };
```

**총계는 본질적으로 조건에 맞는 행을 전부 봐야 합니다.** 5만 건이면 매번 5.7ms 인데
글 목록은 자주 열리는 화면이라 그대로 쌓입니다. 그래서 캐시합니다 — 인자(필터 조합)가
곧 캐시 키라 탭·말머리·정렬별로 따로 잡힙니다.

```diff
+ const countPosts = cacheReference(countPostsUncached, 'post-count', 30_000);
+
+ function invalidatePostCounts(): void {
+   clearReferenceCache('post-count');
+ }
```

**풀 슬롯을 아끼려던 원래 의도는 캐시가 대신합니다.** 캐시가 맞으면 `countPosts` 는
DB 를 치지 않으므로 요청당 쿼리는 2개(목록 + 공지)로, 창 함수 때와 같습니다.
빗나갈 때만 3개가 됩니다.

### 총계가 낡지 않게

글이 늘거나 줄거나 분류가 바뀌면 총계가 달라지므로, 쓰기 경로 넷에서 캐시를 비웁니다.

```diff
  // createPost / updatePost / deletePost / deletePostAsAdmin
+ invalidatePostCounts();
```

어느 필터 조합이 영향을 받는지 알 수 없어 `post-count` 이름 전체를 비웁니다.
그래서 `clearReferenceCache` 가 이름 단위 삭제를 받도록 넓혔습니다.

TTL 30초가 실제로 쓰이는 건 **추천 수가 바뀌어 '인기글' 기준선을 넘나드는 경우**
정도입니다. 추천/추천취소는 총계 무효화를 걸지 않았습니다 — 자주 일어나는데 페이지
수에 영향은 거의 없어서, 최대 30초 늦게 반영되는 편을 택했습니다.

### 빈 페이지 폴백이 필요 없어졌습니다

창 함수는 행이 하나도 없으면 총계가 실릴 자리가 없어서, 뒤 페이지가 비었을 때만
따로 세는 분기를 뒀었습니다. 이제 총계를 항상 따로 구하므로 그 분기가 사라졌습니다.

### 공지는 왜 안 합쳤나

> **⚠ 이 판단은 2026-09-08 에 뒤집혔습니다 — 2부 7번을 보세요.** 아래 근거 자체는
> 틀리지 않았지만, **왕복 하나가 얼마인지** 모르는 상태에서 쓴 것입니다.

`UNION ALL` 로 한 쿼리로 만들 수는 있습니다. 안 했습니다.

공지는 **필터를 타지 않는 별도 목록**입니다. WHERE 도 ORDER 도 LIMIT 도 다르고,
`pinNotices` 가 false 면 아예 나가지도 않습니다(챗봇 검색 경로가 그렇습니다).
합치면 원래 코드가 길게 설명해 둔 "공지가 왜 따로여야 하는가" 가 SQL 속에 묻힙니다.

---

## 4. 글이 많아졌을 때를 위한 인덱스

`db/migrate-042-posts-scale-indexes.sql`. 5만 건 기준 실측입니다.

| 대상 | 인덱스 없음 | 인덱스 적용 |
|---|---|---|
| 챗봇 검색 `ILIKE '%말%'` | 416.2ms | **1.5ms** |
| '인기글' 정렬 | 9.0ms | **0.074ms** |

**'인기글' 정렬** — 최신순은 `posts_recent_idx` 가 받아 주는데 인기순은 받아 줄
인덱스가 없어 전부 읽고 정렬한 뒤 20건만 잘라내고 있었습니다. 정렬 키 순서가
`ORDER.popular` 과 같아야 인덱스를 탑니다.

```sql
CREATE INDEX IF NOT EXISTS posts_popular_idx
  ON posts (like_count DESC, comment_count DESC, created_at DESC);
```

**검색** — `ILIKE '%말%'` 는 앞에 와일드카드가 있어 어떤 btree 도 못 탑니다.
trigram GIN 은 글자를 3개씩 쪼개 넣어 두므로 가운데 낀 말도 찾습니다.
**쿼리는 한 글자도 안 고칩니다** — 플래너가 알아서 Bitmap Index Scan 으로 바꿉니다.

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE INDEX IF NOT EXISTS posts_title_trgm_idx ON posts USING gin (title gin_trgm_ops);
CREATE INDEX IF NOT EXISTS posts_body_trgm_idx  ON posts USING gin (body  gin_trgm_ops);
```

> 검색은 **HTTP 라우트에 없습니다.** `parsePostQuery` 가 `q` 를 읽지 않고,
> `listPosts({ q })` 를 직접 부르는 챗봇(`lib/chat-tools.ts`)만 이 경로를 씁니다.
> 게시판에 검색창이 생기면 그때 이 인덱스가 화면에서도 값을 합니다.

**PGlite 폴백을 깨지 않게** 확장 생성을 `DO` 블록으로 감쌌습니다. `pg_trgm` 이 없으면
검색 인덱스만 건너뛰고 지금까지처럼 전체 스캔으로 동작합니다 — 없으면 느릴 뿐
틀리지는 않습니다. 확장 실패로 마이그레이션 전체가 죽으면 폴백 DB 가 아예 안 뜹니다.

> ⚠ 운영 DB 가 이미 커진 뒤라면 `CREATE INDEX` 가 쓰기를 막습니다. 그때는 이 파일을
> 돌리지 말고 psql 에서 `CONCURRENTLY` 로 따로 만드세요 (`CONCURRENTLY` 는 트랜잭션
> 안에서 못 돌아 마이그레이션 파일에 넣을 수 없습니다).

### 아직 O(표 크기)로 남아 있는 것

- **깊은 페이지** — `OFFSET 40000` 은 4만 행을 세어 버리고 20건을 줍니다(29.0ms).
  커서 방식이면 0.069ms 지만 **API 가 페이지 번호에서 커서로 바뀌는 변경**이라,
  번호 이동 UI(`components/Pagination.tsx`)를 포기해야 합니다. 보류했습니다.
- **목록이 본문 전문을 실어 옵니다** — `POST_SELECT` 가 `p.body` 와 `p.body_doc`(JSONB)
  까지 가져옵니다. 행 폭이 867바이트인데 발췌만 쓰면 245바이트입니다. 20건이면 큰
  차이가 아니고, `POST_SELECT` 를 `getPost`(전문이 필요함)와 공유하고 있어 나누는
  비용이 더 큽니다. 보류했습니다.

---

## 측정

프로덕션 빌드(`npm run start`), PostgreSQL 18, 오락실 942 · 게시글 30.
개선 전후 모두 **풀 상한 10** 으로, 캐싱과 쿼리 축소만의 효과입니다.

### 요청당 DB 쿼리

직접 셌습니다 (`pg_stat_database.xact_commit` 증가분 / 요청 수).

| 엔드포인트 | 개선 전 | 개선 후 |
|---|---|---|
| `/api/machines` | 1.0 | **0** |
| `/api/tier` | 5.0 | **2.0** |
| `/api/posts` | 3.0 | **2.0** (총계 캐시 적중 시) |
| 부하 시나리오 전체 (요청당) | 약 2.0 | **1.33** |

### 20 VU · 5분 (실사용에 가까운 구간)

| 지표 | 개선 전 | 개선 후 | |
|---|---|---|---|
| 처리량 | 78.74/s | **92.05/s** | +17% |
| 중앙값 | 146.06ms | **22.77ms** | −84% |
| 평균 | 149.97ms | **30.4ms** | −80% |
| p95 | 287.23ms | **77.93ms** | −73% |
| 반경검색 p95 | 252.06ms | **72.26ms** | −71% |
| 실패 | 0 | **0** | |

풀 상한을 30 으로 올리고 3·4 번까지 얹은 **최종 상태**는 아래입니다 (같은 20 VU).

| 지표 | 개선 전 | 최종 | |
|---|---|---|---|
| 처리량 | 78.74/s | **93.14/s** | +18% |
| 중앙값 | 146.06ms | **17.61ms** | −88% |
| 평균 | 149.97ms | **20.22ms** | −87% |
| p95 | 287.23ms | **39.07ms** | −86% |
| 반경검색 p95 | 252.06ms | **36.98ms** | −85% |
| 실패 | 0 | **0** | |

임계치(p95 &lt; 500ms) 대비 마진이 1.7배에서 **12.8배**로 벌어졌습니다.

임계치(p95 < 500ms) 대비 마진이 1.7배에서 **6.4배**로 벌어졌습니다.

### 200 VU (한계 구간) — 여기서는 풀 상한이 관건입니다

| 구성 | 처리량 | 중앙값 | p95 |
|---|---|---|---|
| 원본 (풀 10) | 244.02/s | 851.51ms | 1.87s |
| 캐싱+쿼리축소 · 풀 10 | 263.78/s | 637.06ms | 1.93s |
| 캐싱+쿼리축소 · 풀 30 | **372.14 / 384.40/s** | 308 / 268ms | 900 / 849ms |
| 캐싱+쿼리축소 · 풀 50 | **337.37 / 414.82/s** | 123 / 130ms | 1.58s / 816ms |

**캐싱만으로는 이 구간에서 처리량이 8% 느는 데 그칩니다.** 풀 상한 10 이 여전히
벽이기 때문입니다. 두 개선은 서로 다른 구간을 담당하므로 함께 적용해야 합니다.

### 풀 상한을 얼마로 할까 — 30 을 권합니다

30 과 50 을 두 번씩 쟀습니다 (200 VU).

| 풀 | 1회차 | 2회차 | 평균 | 편차 |
|---|---|---|---|---|
| 30 | 372.14/s | 384.40/s | 378.3/s | **3.2%** |
| 50 | 337.37/s | 414.82/s | 376.1/s | **20.5%** |

**평균 처리량은 사실상 같고(378 대 376), 편차가 다릅니다.** 커넥션을 더 열수록
Postgres 안에서 경합이 늘어 실행마다 결과가 출렁입니다. 같은 성능이라면 흔들리지
않는 쪽이 낫고, `max_connections = 100` 아래 여유도 더 남습니다(인스턴스를 늘릴 여지).

20 VU 구간에서도 30 이 확실히 낫습니다.

| 풀 | 처리량 | 중앙값 | p95 |
|---|---|---|---|
| 10 | 92.05/s | 22.77ms | 77.93ms |
| 30 | 93.24/s | **18.48ms** | **47.21ms** |

처리량이 제자리인 것은 서버 한계가 아니라 시나리오가 매 반복 1초를 쉬기 때문입니다.

> ⚠ **실행 간 변동이 설정 간 차이만큼 큽니다.** 같은 풀 50 설정에서 337/s 와 414/s 가
> 나왔습니다. 200 VU 수치를 한 번만 재고 비교하면 없는 차이를 만들어냅니다 —
> 최소 두 번씩 재세요. 20 VU 구간은 훨씬 안정적입니다.

---

## 검증

`npm test` 490건, `npm run typecheck` 전부 통과. 그 외에 직접 확인한 것들입니다.

**캐시가 실제로 DB 를 안 친다** — `/api/machines` 20회 요청에 앱발 쿼리 0건.
대조군 `/api/arcades` 는 같은 조건에서 20건 그대로.

**사용자 데이터가 섞이지 않는다** — 투표가 갈리는 채보를 골라 플레이어를 번갈아
호출했습니다.

| 요청 | chart 26 | chart 27 |
|---|---|---|
| DB 실제값 (p1) | 0.85 | 0.67 |
| `playerId=1` | 0.85 ✓ | 0.67 ✓ |
| DB 실제값 (p2) | 0.80 | 1.00 |
| `playerId=2` | 0.80 ✓ | 1.00 ✓ |
| 비로그인 | null ✓ | null ✓ |

1 → 2 → 1 → 2 로 번갈아 불러도 매번 자기 값이 나왔습니다.

**총계와 페이지네이션이 그대로다**

| 요청 | posts | total | hasMore |
|---|---|---|---|
| `?limit=5` | 5 | 29 | true |
| `?limit=5&offset=1000` | 0 | **29** | false |
| `?limit=100` | 29 | 29 | false |

빈 페이지에서도 `total` 이 29 로 유지됩니다 — 폴백이 동작한다는 뜻입니다.
(게시글 30건 중 공지 1건을 뺀 29건이 맞습니다.)

### 다시 재려면

```bash
npm run build
npm run start
```

```bash
k6 run -q -e SCENARIO=load "C:\Users\user\Desktop\claude\개인 프로젝트\arcade-finder\load-test\k6-read.js"
```

부하테스트는 **반드시 프로덕션 빌드로** 재세요. `next dev` 는 수치를 크게 왜곡합니다.
스크립트 경로는 저장소 루트 기준 `arcade-finder/load-test/` 라 절대경로가 안전합니다.

> 재기 전에 **3000 포트에 낡은 서버가 남아 있지 않은지 확인하세요.** 실제로 한 번
> 당했습니다 — 2시간 전에 뜬 프로세스가 포트를 물고 있어서 새 서버가 바인딩에
> 실패했고, 개선된 코드가 아니라 옛 빌드를 측정하고 있었습니다.

---

## 남은 것

- **풀 기본값은 30 으로 반영했습니다.** 30 에서도 커넥션은 상한에 붙어 있지만, 50 으로
  올려도 평균 처리량이 늘지 않고 편차만 커졌습니다. **인스턴스를 여러 개 띄우게 되면
  30 × 인스턴스 수가 `max_connections`(100) 를 넘지 않는지 확인하세요** — 4개부터 넘칩니다.
- **`/api/arcades` 에 `LIMIT` 이 없습니다.** 942행 307KB 를 통째로 반환합니다.
  다만 프론트가 `fullListRef` 에 전체 목록을 담아 재사용하는 구조라, 페이지네이션은
  그 설계를 바꾸는 일입니다. 첫 로드 1회뿐이라 우선순위는 낮습니다.
- **기종 데이터가 비어 있습니다.** `arcade_machines` 가 3행이고 942곳 중 940곳이 보유
  기종 0입니다. `MACHINES_SUBQUERY` 가 지금 싼 건 상관 서브쿼리가 942번 돌면서 전부
  빈 결과를 내기 때문입니다. **데이터를 채우면 이 기준선은 무효가 됩니다** — 그때 다시
  재세요.
- **부하 시나리오가 실사용 패턴이 아닙니다.** 한 반복이 6개 엔드포인트를 동시에
  때리는데, 실제 사용자는 한 페이지에 머뭅니다. 페이지별 시나리오로 나누면 더
  현실적인 수치가 나옵니다.


---
---

# 2부 — 지표가 거짓말한 이유, 그리고 프로세스 (2026-09-08)

1부는 "**느린 쿼리 아님**" 으로 결론 내고 넘어갔습니다. 그런데 그 뒤에 붙인 계측
(`lib/telemetry.ts`)의 슬로우 쿼리 목록에는 `SELECT posts` · `GET /api/tier` ·
`GET /api/posts` 가 계속 올라왔습니다. 1부의 결론과 지표가 반대로 말한 셈입니다.

**지표가 맞고 1부도 맞았습니다.** 지표가 재던 것이 SQL 시간이 아니었을 뿐입니다.
2부는 그 모순을 풀고, 거기서 나온 세 가지 변경(5·6·7)을 기록합니다.

---

## 5. 지표를 셋으로 쪼갰습니다 — 대기 · 실행 · 루프

### 증거 — `SELECT 1` 하나짜리 라우트가 가장 느렸습니다

동시 40 요청, `next dev`:

| 라우트 | p50 | p95 | max |
|---|---|---|---|
| **`/api/health`** (쿼리 `SELECT 1` **한 개**) | 242ms | **3,056ms** | 3,599ms |
| `/api/posts?limit=20` | 316ms | 426ms | 453ms |
| `/api/tier?machineId=1` | 319ms | 355ms | 361ms |

그 `SELECT 1` 은 Postgres 로그상 **0.100ms** 입니다. 쿼리가 하나뿐이고 그 하나가
0.1ms 인 라우트가 3초일 수는 없습니다. 확인 사살로 `log_min_duration_statement = 200`
을 걸고 같은 부하를 줬더니 **Postgres 가 잡은 느린 문장은 0건**이었습니다.

같은 순간 node 는 **코어 1개의 95%** 를 쓰고 있었습니다(22코어 중 1개). 요청당 CPU 는
`/api/health` 기준 **10.75ms** — 작은 JSON 하나 만드는 데 쓸 값이 아닙니다. 프로세스는
`next dev` 였고, 같은 커밋·같은 라우트·같은 응답(10,079B)으로 프로덕션과 비교하면:

| `/api/posts?limit=20` | `next dev` | `next start` |
|---|---|---|
| p50 | 356ms | **63ms** |
| p95 | 527ms | **88ms** |
| 요청당 CPU | 12.54ms | **2.70ms** |

1부 마지막의 "부하테스트는 반드시 프로덕션 빌드로" 와 같은 함정인데, **지표를 통해
보면 DB 문제로 보인다**는 것이 새로 배운 점입니다.

### 왜 그렇게 보였나

`withTelemetry` 는 이렇게 잽니다:

```ts
const t0 = performance.now();
await run();                    // → db.query → pool.query (슬롯 획득 + 실행 + 반납)
recordQuery(sql, performance.now() - t0, true);
```

`await pool.query` 를 통째로 감싸므로 잰 값에 ① **풀 슬롯 대기** ② **이벤트 루프 재개
지연** 이 함께 들어갑니다. 노드가 한 코어를 다 쓰고 있으면 `await` 가 깨어나는 시점
자체가 밀리고, 그 지연이 전부 쿼리 시간으로 기록됩니다. 목록 맨 위에 `SELECT posts` 가
오는 이유도 느려서가 아니라 **가장 자주 실행돼 표본이 많아서** 입니다.

### 어떻게 쪼갰나

pg 어댑터를 `pool.query` 한 방에서 `connect` → `query` → `release` 로 펼쳤습니다
(`lib/db.ts`). 동작은 동일합니다 — `pool.query` 자체가 이 셋을 묶어 놓은 것이고,
실패해도 `finally` 에서 반납합니다.

```diff
  async query(text, params) {
-   const res = await pool.query(text, params as never[]);
-   return { rows: res.rows };
+   const t0 = performance.now();
+   const client = await pool.connect();
+   const t1 = performance.now();
+   try {
+     const res = await client.query(text, params as never[]);
+     recordPoolQuery(text, t1 - t0, performance.now() - t1);
+     return { rows: res.rows };
+   } finally {
+     client.release();
+   }
  }
```

이벤트 루프 지연은 `monitorEventLoopDelay` 로 따로 잽니다(`lib/telemetry-node.ts`).
히스토그램은 C++ 쪽에서 채우므로 이벤트 루프에 얹히는 비용이 없고, `globalThis` 에
두어 `snapshot()` 이 읽습니다 — Next 가 instrumentation 번들과 앱 번들을 따로 만들어서
모듈 스코프에 두면 못 읽습니다(`__pulseTelemetry` 와 같은 이유).

**새 지표**

| 이름 | 뜻 |
|---|---|
| `app.db.p95_ms` | (종전) DB 를 부른 코드가 **체감한** 시간 — 뜻을 바꾸지 않았습니다 |
| `app.db.wait_p95_ms` | 풀 슬롯 대기만 |
| `app.db.exec_p95_ms` · `app.db.exec_slow` | 순수 실행만 |
| `query.<지문>.exec_ms` | 지문별 순수 실행 p95 |
| `app.loop.p95_ms` · `app.loop.max_ms` | 이벤트 루프 지연 |

> `query.<지문>` 쪽이 `exec_p95_ms` 가 아니라 `exec_ms` 인 이유는 이름 64자 예산입니다:
> `query.`(6) + 키 48 + `.exec_p95_ms`(12) = 66 으로 넘칩니다. 값은 p95 입니다.

**읽는 순서**

- `app.loop.p95_ms` 가 높다 → **프로세스 포화**. 쿼리는 무죄입니다.
- `loop` 는 낮은데 `exec` 가 높다 → **진짜 쿼리·DB 문제**입니다.
- `wait` 만 높다 → **풀 상한이나 요청당 쿼리 수**입니다.

> **⚠ `exec` 와 `wait` 도 루프 지연에서 자유롭지 않습니다.** `await client.query()` 와
> `await pool.connect()` 의 재개 역시 이벤트 루프를 통과합니다. 그래서 셋을 **반드시
> 나란히** 봐야 하고, 그것이 루프 지연을 같이 넣은 이유입니다.

### 무엇이 보이게 됐나

200 VU, 프로덕션, 단일 프로세스, 풀 30 을 두 번:

| | #1 | #2 |
|---|---|---|
| DB 체감 p95 | 1,004.9ms | 885.5ms |
| └ **풀 대기 p95** | **906.7ms** | **860.9ms** |
| └ **순수 실행 p95** | **39.1ms** | **48.7ms** |
| 루프 지연 p95 | 54.7ms | 55.5ms |
| 느린 쿼리 (체감 250ms+) | 16,868 | 16,899 |
| **느린 쿼리 (실행 250ms+)** | **468** | **482** |

체감 1초의 **90%가 큐 대기**이고, 느린 쿼리는 **36배 과대계상**이었습니다. 지문별로도
전부 890~935ms 에 몰려 있고 실행은 25~41ms 입니다 — 특정 쿼리가 느린 게 아니라
**다 같이 줄을 서 있다**는 뜻입니다. 어느 하나가 진짜 문제면 그 `exec` 만 튑니다.

그리고 1부의 "풀 30 이 적정" 결론이 **왜** 맞는지가 보입니다. 30 → 80 으로 올려 보면:

| | pool=30 | pool=80 |
|---|---|---|
| 처리량 | 332.9 / 338.4 req/s | 361.0 req/s (+8%) |
| 실패율 | 0% | **2.89%** |
| 풀 대기 p95 | 906.7 / 860.9ms | 842.5ms (거의 그대로) |
| **순수 실행 p95** | 39.1 / 48.7ms | **286.2ms (7배)** |
| 느린 쿼리(실행) | 468 / 482 | 1,162 |

**대기가 Postgres 안으로 옮겨간 것**입니다. 처리량은 +8% 인데 실행이 7배로 뜁니다.
쪼개기 전이라면 `app.db.p95_ms` 가 880 근처에서 안 움직이니 "변화 없음" 으로
오독했을 상황입니다. (실패 2.89% 는 앱에 닿지 못한 소켓 단계 실패입니다 —
계측 5xx = 0, 서버 로그 무에러.)

---

## 6. 프로세스를 늘렸습니다 — 22코어 중 1개만 쓰고 있었습니다

노드는 싱글 스레드입니다. 부하 중 이 머신은 코어 1개의 50%(무-think-time 폭주에서는
83%)만 쓰고 있었고, 나머지 21코어는 놀았습니다. 풀도 쿼리도 아니라 **프로세스 수**가
다음 한 걸음이었습니다.

### 구성

`next start` 인스턴스 8개(3101~3108), **프로세스당 `PG_POOL_MAX=11`** — 합 88 입니다.
1부 "남은 것" 의 경고가 그대로 걸립니다: `max_connections = 100` 이 하드 리밋이라
**프로세스 수 × 풀** 이 여기 묶입니다.

앞단 프록시는 두지 않고 k6 쪽에서 VU 를 포트에 나눴습니다. 이 머신에 nginx·caddy·
haproxy 가 없고, **Node 로 프록시를 만들면 그게 단일 스레드라 지금 재려는 천장이
프록시로 그대로 옮겨옵니다.** 클라이언트에서 나누면 "프로세스를 늘리면 앱이 얼마나
받아내는가" 만 남습니다.

> k6 사본에서 `BASE` 를 상수로 계산하면 안 됩니다 — **init 문맥에서 `__VU` 는 0** 이라
> 포트가 `undefined` 가 됩니다. 함수로 두어 iteration 안에서 정해지게 해야 합니다.

### 측정 (k6 stress, 200 VU, 각 2회)

| | 1 프로세스 pool=30 | 8 프로세스 pool=11 |
|---|---|---|
| 처리량 | 332.9 / 338.4 req/s | **550.2 / 552.1 req/s** |
| **p95** | 1.34s / 1.28s | **104ms / 87ms** |
| 임계치 p95 &lt; 500ms | ✗ | **✓** |
| 실패 | 0% | 0% |
| node CPU | 1코어의 50% | **1.4코어 합** (22코어 중 6%) |
| 풀 대기 p95 | ~880ms | **6.3ms** |
| 순수 실행 p95 | 39~49ms | 16.5ms |
| 루프 지연 p95 | 55ms | 22ms |
| 느린 쿼리(실행) | 468 / 482 | **0** |

**p95 가 12배 좋아지고 느린 쿼리가 0 이 됩니다.** 임계치를 처음 통과했습니다.
처리량이 +63% 에 그친 이유는 서버가 아니라 부하 쪽입니다 — 8개 합쳐 1.4/22 코어만
쓰고 p95 가 87ms 이니, 200 VU × think time 1s 로는 채울 수 없습니다.

### 천장은 어디인가

think time 을 뺀 포화 시나리오로:

| | 처리량 | p95 | 실패 | node CPU |
|---|---|---|---|---|
| 8프로세스 pool=11, 200 VU | **1,745/s** | 674ms | 0% | 5.7코어 |
| 8프로세스 pool=11, **400 VU** | 1,615/s | 1.61s | 0% | 6.0코어 |
| 8프로세스 pool=6 (합 48), 200 VU | 1,387/s | 891ms | 0% | 4.9코어 |
| 1프로세스 pool=11, 200 VU | (1,177/s) | 1.79s | **46.3%** | 1.1코어 |

- **VU 를 200 → 400 으로 두 배 늘려도 처리량이 안 오르고 p95 만 2배** → ~1,700 req/s
  가 실제 천장입니다. k6 자신은 1.2~1.3코어만 써서 부하 생성기 한계가 아닙니다(확인함).
- **단일 프로세스는 무-think-time 200 VU 를 아예 못 받습니다** — 실패 46.3%, 소켓 수락
  단계입니다. 그래서 그 1,177/s 는 거절을 센 값이라 비교 대상이 아닙니다.
- 총 풀 48 → 88(+83%) 이 처리량 +26% → 풀이 제약이긴 하나 **수익이 체감**합니다.
- 포화 중에도 Postgres `active` 는 88개 중 **1~14개** 입니다. 슬롯을 앱이 **체크아웃한 채**
  JS 차례를 기다리는 시간이 길고, 그동안 Postgres 는 그 커넥션을 `idle` 로 봅니다.
  슬롯 점유 ≈ 30 ÷ 615 q/s ≈ 49ms, 그중 SQL 은 3ms — 나머지가 루프 지연과 맞습니다.

### 배포할 때

- **인스턴스 여러 개 + 멀티스레드 프록시.** 프록시가 단일 스레드면 천장이 그대로 옮겨옵니다.
- `PG_POOL_MAX × 인스턴스 수 ≤ max_connections − 여유`. 8 × 11 = 88 이 이 장비의 상한선입니다.

---

## 7. `listPosts` 의 공지를 합쳤습니다 — 위 판단을 뒤집습니다

1부 [공지는 왜 안 합쳤나](#공지는-왜-안-합쳤나) 에서 "안 했습니다" 라고 적었습니다.
근거는 **읽기 어려워진다**는 것이었고, 그 자체는 틀리지 않았습니다. 다만 그때는
**왕복 하나가 얼마인지** 몰랐습니다.

5번에서 확인된 것이 답을 바꿉니다: 쿼리 하나가 점유하는 슬롯 시간은 실제 실행 3ms 가
아니라, 슬롯을 잡은 채 이벤트 루프 차례를 기다리는 **20~50ms** 입니다. 그러면
**왕복 수가 곧 비용**입니다. 읽기 어려워지는 문제는 주석으로 갚을 수 있고, 슬롯은
그럴 수 없습니다.

### 어떻게

목록 쿼리에 공지 갈래를 `UNION ALL` 로 붙였습니다. `$9::boolean` 이 false 면 공지
갈래가 행을 내지 않으므로, **예전에 쿼리를 아예 보내지 않던 경우(`pinNotices=false`)와
결과가 같습니다.** 총계는 넣지 않았습니다 — 그쪽은 3번(창 함수 되돌리기)에서 정리한
대로 캐시가 맞습니다.

> **⚠ `UNION ALL` 은 갈래 간 행 순서를 보장하지 않습니다.** Append 가 순서대로 붙이는
> 것은 구현 사실일 뿐이고 병렬 Append 가 걸리면 섞입니다. 갈래별로 SQL 과 **같은 키**로
> JS 에서 다시 세웁니다(많아야 20 + 공지 몇 개라 비용 없음). 정렬 키 표는 SQL 과 JS 가
> **공유**합니다 — 두 곳에 따로 적으면 한쪽만 고쳐져 목록이 조용히 뒤섞입니다.

실제 감소는 3 → 1 이 아니라 **2 → 1** 입니다. 총계는 이미 캐시 적중이라 대부분의
요청에서 DB 를 치지 않았습니다(1부 "요청당 DB 쿼리" 표의 `2.0 (총계 캐시 적중 시)`).

| `/api/posts` | 개선 전(1부) | 2부 |
|---|---|---|
| 요청당 트랜잭션 (직접 계수) | 1.90 | **0.97** |
| 부하 중 요청당 `SELECT posts` (200 VU) | 2.00 | **1.00** |

### 측정 — 200 VU, BEFORE/AFTER 각 2회 번갈아

| | BEFORE | AFTER | |
|---|---|---|---|
| 처리량 | 313.1 req/s | **333.9 req/s** | +6.6% |
| 중앙값 | 377.4ms | **318.5ms** | −15.6% |
| p90 | 1,350ms | **1,135ms** | −15.9% |
| **p95** | 1,620ms | **1,340ms** | **−17.3%** |
| 실패 | 0% | 0% | |

라운드별로 짝지어도 방향이 같습니다 — r1 처리량 +4.5% / p95 −17.8%,
r2 +8.9% / p95 −16.8%.

200 VU 구간 지표 중앙값이 이유를 말해 줍니다:

| | BEFORE | AFTER | |
|---|---|---|---|
| `SELECT posts` /분 | 7,034 | 3,704.7 | **−47%** |
| **풀 대기 p95** | 600.0ms | **511.4ms** | **−15%** |
| 순수 실행 p95 | 91.8ms | 84.2ms | −8% |
| 루프 지연 p95 | 57.5ms | 53.7ms | −6% |
| `GET /api/posts` p95 | 617.1ms | 552.0ms | −11% |
| 느린 쿼리(체감) | 10,278 | 7,303 | −29% |
| 전체 쿼리/분 | 28,115 | 25,989 | −8% |

**요청당 2.00 → 1.00 이 정확히 절반이고, 그 절감이 풀 대기 −15% 로 나타납니다.**
왕복 하나를 줄인 것이 슬롯 큐를 직접 짧게 만들었습니다.

`/api/posts` 만 때리는 부하로는 이득이 훨씬 큽니다 — 30 VU 포화에서
**388 → 465 req/s (+20%)**, p95 **94 → 77ms**. 8라운드 전부 AFTER 가 빨랐습니다
(+9, +17, +26, +16, +26, +12, +17, +21%). 전체 벤치에서 +6.6% 로 줄어드는 것은
`/api/posts` 가 6개 엔드포인트 중 하나라서 — **희석**입니다.

### 검증

- `/api/posts` 응답을 필터 **12조합**(전체·게임탭 2·공지·말머리+탭·인기글·검색·
  페이징 2·playerId·인기글+말머리·복합)으로 편집 전후 비교 → **바이트 단위 동일**
- `tsc --noEmit` 통과, 테스트 **557개 전부 통과**
- 쓰이지 않게 된 `listNotices` 제거(참조 0)

---

## 2부의 측정 방법 — 이 머신에서 A/B 를 재는 법

**⚠ 처리량 A/B 는 짝지어 번갈아 재세요.** 순차로 재면 드리프트가 효과를 완전히 덮습니다.
7번을 처음 순차로 쟀을 때 같은 BEFORE 코드가 **631 → 616 → 463 req/s** 로 흘러내렸고,
그 상태로 비교해서 "**AFTER 가 19.6% 느리다**" 는 반대 결론이 나왔습니다. 원인은 배경
프로세스입니다(OneDrive.Sync.Service CPU 8,882초, Docker Desktop, 다른 세션).

→ **BEFORE/AFTER 를 각자 포트에 동시에 띄우고 20~40초씩 번갈아, 라운드별 비율의
중앙값**을 쓰세요. 드리프트가 양쪽에 똑같이 걸려 상쇄됩니다.

**⚠ 혼합 벤치의 `app.db.qpm / app.http.rpm` 으로 "요청당 쿼리" 를 보면 안 됩니다.**
6개 엔드포인트 평균이라 7번 전후가 **양쪽 다 1.33** 으로 나와 변화가 안 보입니다.
라우트·지문별(`query.SELECT posts.qpm ÷ route.GET /api/posts.rpm`)로 나눠야 2.00 → 1.00
이 드러납니다.

**200 VU 변동은 풀 30 에서는 작습니다** — 332.9 vs 338.4 req/s (1.7%). 1부의
"풀 50 에서 337 vs 414(20%)" 경고는 그 설정에 한정된 이야기입니다. 다만 한 번만
재지는 마세요.

**Postgres CPU 는 `Get-Process postgres` 로 안 읽힙니다**(서비스 계정). 대신
`pg_stat_activity` 의 `state` 분포를 보세요 — `active` 개수가 실제 작업량입니다.

---

## 남은 것 (2부)

- **`app.db.p95_ms` 의 뜻을 바꾸지 않았습니다.** 기존 대시보드가 그대로 살아 있어야
  해서인데, 그 값만 보면 여전히 "DB 가 느리다" 로 읽힙니다. 대시보드에 `wait`·`exec`·
  `loop` 셋을 나란히 올려 두는 것이 이 작업의 마지막 절차입니다.
- **천장 ~1,700 req/s 는 풀 슬롯 + 프로세스별 루프 지연의 혼합입니다.**
  `max_connections` 상향이나 pgBouncer 가 다음 수순이지만 총 풀 +83% 가 처리량 +26%
  인 체감 구간입니다. 같은 비용이면 **요청당 쿼리 수를 더 줄이는 쪽**이 낫습니다
  (`/api/tier` 가 요청당 2.0 으로 남아 있습니다).
- **두 개선(6·7)은 같은 병목을 나눠 먹습니다.** 8프로세스 구성에서는 풀 대기가 이미
  6.3ms 이므로, 7번의 쿼리 절감 체감은 단일 프로세스(−17.3%)보다 훨씬 작습니다.
  순서를 정해야 하면 **프로세스 늘리기가 압도적으로 큰 한 방**입니다.
- **프록시를 아직 안 재봤습니다.** 8프로세스 수치는 클라이언트 분산 기준이라, 실제
  프록시를 앞에 두면 그만큼 깎입니다. 프록시를 고르고 나서 다시 재야 합니다.

---

# 3부 — 왕복을 더 줄이고, 풀 상한을 다시 재다 (2026-09-09)

2부가 "왕복 수가 곧 비용" 이라는 것을 밝혔으므로, 남아 있던 가장 뚱뚱한 경로를 같은
방식으로 줄였습니다. 그리고 그 결과로 **1부가 정한 풀 상한의 근거가 유효한지** 다시
쟀습니다 — 쿼리를 줄이면 풀이 덜 아쉬워지므로, 그 판단은 코드가 바뀔 때마다 갱신
대상입니다.

측정은 2부의 방법 그대로입니다(각자 포트에 동시 기동 → 30초씩 번갈아 → 라운드별
비율의 중앙값). 다만 이번에는 그 판을 스크립트로 못박았습니다:

- `load-test/k6-post-detail.js` — `GET /api/posts/:id` **하나만** 때립니다.
  `k6-read.js` 는 6개 엔드포인트를 섞으므로 한 라우트의 개선이 1/6 로 희석됩니다.
- `load-test/bench-server.mjs` — distDir · 포트 · 풀 상한을 인자로 받는 프로덕션 서버.
- `next.config.mjs` 의 `distDir` 을 `NEXT_DIST_DIR` 로 뺐습니다. BEFORE/AFTER 두 빌드가
  동시에 존재해야 번갈아 잴 수 있습니다.

## 8. 글 상세 — 세 번 가던 것을 한 번으로

### 무엇이 문제였나

`GET /api/posts/:id` 는 DB 를 세 번 다녀왔습니다.

```
await bumpView(id)                     -- UPDATE posts SET view_count = view_count + 1
await getPost(...)  →  POST_SELECT     -- 글 한 행
                    →  Promise.all(댓글 한 페이지, 첨부 목록)
```

앞의 둘이 **순차**인 이유는 하나뿐이었습니다 — 댓글 페이지 offset 을 범위 안으로
당기려면(`clampOffset`) `comment_count` 를 먼저 알아야 했습니다. 조회수 UPDATE 는
그보다 앞에서, 읽기와 아무 관계없이 기다리고 있었습니다.

### 어떻게

offset 보정을 SQL 로 옮기면 셋이 한 문장에 들어갑니다. 정수 나눗셈이 내림이라
`clampOffset` 과 같은 계산이 한 줄로 떨어집니다 — 댓글 15개·페이지 10 이면 마지막
페이지 시작은 `(14/10)*10 = 10`.

```sql
WITH bumped AS (
  UPDATE posts SET view_count = view_count + 1
   WHERE id = $2::int AND $5::boolean          -- view=1 이 아니면 한 행도 안 건드린다
   RETURNING id, view_count
),
base AS (
  SELECT src.*, b.view_count AS view_count_bumped,
         LEAST(GREATEST($3::int, 0),
               (GREATEST(src.comment_count - 1, 0) / $4::int) * $4::int) AS comment_offset
    FROM (<POST_SELECT> WHERE p.id = $2::int) src
    LEFT JOIN bumped b ON b.id = src.id
)
SELECT base.*,
       (댓글 한 페이지 json_agg), (첨부 목록 json_agg)
  FROM base
```

⚠ **데이터 변경 CTE 의 결과는 같은 문장의 다른 부분에 보이지 않습니다.** 스냅샷이
문장 시작 시점에 고정되므로 `p.view_count` 를 그냥 읽으면 **올리기 전** 값이 나옵니다
(실측: base 431 · RETURNING 432). 나눠 보내던 때와 응답이 같아야 하므로 조회수는
`bumped` 의 RETURNING 쪽을 씁니다.

json_agg 에는 서브쿼리의 `ORDER BY` 와 **같은 키를 다시** 줍니다. 서브쿼리 정렬이
집계 입력 순서로 이어지는 것은 구현 사실일 뿐입니다 (7번의 UNION ALL 과 같은 이유).

### 측정 — 200 VU · 풀 35 · 30초씩 8쌍 번갈아

| 지표 (라운드 중앙값) | BEFORE | AFTER | 비율 중앙값 |
|---|---|---|---|
| 처리량 | 418.9/s | **543.3/s** | **+30.2%** |
| 중앙값 | 459.2ms | **357.9ms** | −25.5% |
| p90 | 580.2ms | **450.9ms** | −20.3% |
| **p95** | 606.5ms | **467.3ms** | **−19.4%** |
| p99 | 701.4ms | **498.9ms** | −21.7% |
| 총 처리 요청 (8라운드 합) | 106,525 | **130,397** | +22.4% |
| 실패 · check 실패 | 0 · 0 | 0 · 0 | |

8라운드 중 **6라운드가 AFTER 우세**(+15.7 · +28.7 · +31.7 · +40.3 · +43.2 · +54.7%)이고
r4·r5 는 반대(−11.5 · −22.5%)로 나왔습니다. 그 둘은 **붙어 있고 양쪽 처리량이 같이
떨어진** 구간이라 변경의 성질이 아니라 드리프트로 읽습니다 — 2부의 경고 그대로입니다.

### 검증

- BEFORE 서버(3100)와 AFTER 서버(3200)의 응답을 **16경우** 비교 → 바이트 단위 동일
  (offset 0·10·999 클램프 · 첨부 있는 글 · 댓글 0 · playerId · 없는 글 404 · 잘못된 id 400
  · 목록 · 검색 · 인기순). `?view=1` 은 양쪽 다 정확히 +1.
- `tsc --noEmit` 통과, 테스트 557개 통과. 참조가 0 이 된 `bumpView`·`listComments` 제거.
- 부하가 올린 조회수는 스냅샷으로 30건 전부 복원했습니다 (실제 경로를 때렸으므로).

### 클라이언트도 같이 — 이미 아는 것을 감추지 않기

목록은 제목·글쓴이·시간·추천/댓글/조회 수를 **이미** 갖고 있는데 상세는 스피너부터
띄웠습니다. 이제 그 요약으로 즉시 그리고 본문·댓글 자리만 스켈레톤으로 둡니다. 그리고
마우스를 누른 순간(`pointerdown`) 상세가 어차피 보낼 요청을 미리 시작합니다 — 키별로
하나만 나가므로(`lib/post-cache.ts`) 요청 수는 그대로고 시작만 빨라집니다.

hover 가 아니라 pointerdown 인 이유: hover 로 미리 받으려면 조회수를 올리지 않는 별도
요청이 필요해져 결국 두 번 나갑니다. 터치는 걸지 않습니다 — 손가락이 닿는 것은 스크롤의
시작일 수도 있어 그것으로 조회수를 올릴 수 없습니다.

| | BEFORE | AFTER |
|---|---|---|
| 재방문 시 상세 요청 | 1회 | **0회** (담아 둔 것, 15초 안) |
| 클릭 → 본문 (localhost) | 16ms | 9ms |

⚠ **이 개선의 크기는 localhost 에서 드러나지 않습니다.** 왕복 지연이 사실상 0이라
BEFORE 도 16ms 입니다. 첫 화면이 RTT 와 무관해지는 것이 이 변경의 값이므로, 배포 뒤
실사용 RTT 에서 다시 재야 합니다.

### 되돌린 것 — 상세일 때 목록을 언마운트하지 않기

"뒤로 올 때 목록을 다시 받아 온다" 고 보고 감추기(`hidden`)로 바꿨는데, 재보니 근거가
틀렸습니다: 목록 데이터는 부모 컴포넌트의 state 라 애초에 사라지지 않고, 뒤로가기는
이미 **9ms 에 스피너 없이** 그려지며 스크롤 위치도 브라우저가 popstate 에서 복원합니다.
상세를 열 때 나가는 목록 재조회도 양쪽이 똑같이 한 번입니다. 얻는 것이 없어 되돌렸습니다.

## 9. 풀 상한을 다시 재다 — 30 을 유지합니다

1부에서 30 을 고른 근거는 "50 은 평균이 같은데 편차가 크다(3.2% 대 20.5%)" 였습니다.
그 측정은 **요청당 2~3 쿼리** 시절의 것이라, 8번으로 요청당 1 쿼리가 된 지금도 같은
결론인지 확인해야 했습니다.

### 200 VU · 30초씩 6쌍 번갈아 (같은 빌드, 풀 상한만 다르게)

| | 처리량 (중앙값) | p95 (중앙값) | 총 요청 (6라운드) | 라운드 편차 |
|---|---|---|---|---|
| 풀 30 | 765.1/s | **338.2ms** | **127,150** | 19.0% |
| 풀 35 | 758.6/s | 362.9ms | 123,943 | 18.6% |

→ 풀 35 는 30 대비 처리량 **비율 중앙값 ×0.953 (−4.7%)**, 6라운드 중 5라운드에서 30 이
높았습니다. 크기가 작고 편차가 19% 이므로 "30 이 빠르다" 보다 **"35 로 올려 얻는 것이
없다"** 로 읽는 것이 정확합니다.

| | 처리량 (중앙값) | p95 (중앙값) | 총 요청 (6라운드) | 라운드 편차 |
|---|---|---|---|---|
| 풀 35 | 720.1/s | 430.5ms | 125,426 | 11.8% |
| 풀 50 | 700.2/s | **359.8ms** | 125,901 | 10.8% |

→ 처리량 **비율 중앙값 ×1.000 (0.0%)**, 중앙값 지연도 +0.4% 로 같습니다. p95 만 −22.9%
(6라운드 중 4라운드에서 −21~−31%, r5·r6 은 +27 · +10%)로 꼬리가 조금 짧아집니다.
**1부의 "50 은 출렁인다" 는 이 조건에서 재현되지 않았습니다** (10.8% 대 11.8%).

### 왜 세 값이 다 비슷한가

부하 중 `pg_stat_activity` 를 표본으로 뜨면 **실행 중인 백엔드가 어느 설정에서도 최대
2개**(평균 1.0~1.1)입니다. 요청당 1 쿼리에 실행 시간이 1ms 대이므로, 500~800 req/s 에서도
동시에 일하는 백엔드는 한 개 남짓입니다. 슬롯은 30 개도 남아돌고, 남은 병목은 노드
이벤트 루프입니다 — **1부의 "풀 상한이 관건인 구간" 을 이미 지나왔습니다.**

### 비용 — 슬롯 하나가 얼마인가

풀 슬롯은 곧 Postgres 백엔드 프로세스입니다. 두 번 재서 일치했습니다.

| 상태 | 프로세스 | 총 WorkingSet |
|---|---|---|
| 유휴 (접속 1) | 10개 | 121MB |
| 부하 중 (접속 85 = 35+50) | 95개 | 2,054MB |
| 부하 중 (접속 30) | 40개 | 827MB |

→ **접속 1개당 22.7 · 23.5MB** (Windows 백엔드는 Linux fork 보다 무겁습니다)

| | 피크 Postgres RAM | `max_connections=100`(예약 3 → 97) 안에서 앱 인스턴스 |
|---|---|---|
| **풀 30** | **682MB** | **3개** (90, 여유 7) |
| 풀 35 | 796MB | 2개 (70) |
| 풀 50 | 1,137MB | 1개 (50) — 2개면 100 으로 초과 |

부하가 끝나면 12초 안에 회수되어 122MB 로 돌아옵니다(`idleTimeoutMillis` 기본 10초).
상시 비용은 아니지만 용량 산정은 피크로 해야 합니다. 관리형 Postgres 에서는 접속 한도가
인스턴스 메모리에서 파생되므로, 상한을 올리는 것은 곧 한 단계 큰 요금제를 쓰는 것입니다.

### 결론

**30 을 유지합니다** (`lib/db.ts` 의 기본값, `PG_POOL_MAX` 미설정 상태). 근거가 1부와
달라졌다는 점만 기록해 둡니다 — 그때는 *편차가 작아서* 30 이었고, 지금은 *30~50 사이에
처리량 차이가 없어서* 그중 가장 싼 값이 30 이기 때문입니다. 꼬리(p95)를 줄이려면 슬롯을
늘리는 대신 요청당 쿼리를 더 줄이는 쪽이 같은 비용에서 낫습니다.

그리고 2부 6절의 8프로세스 계획으로 가면 풀 상한은 **인스턴스당 예산**이라 97 ÷ 8 ≈ 12
로 오히려 내려야 합니다. 상한을 올리는 방향은 그 계획과 정면으로 부딪힙니다.

## 남은 것 (3부)

- **`/api/tier` 가 아직 요청당 2.0 쿼리입니다.** 8번과 같은 방식(한 문장으로 접기)이
  그대로 통하는 자리이고, 9번의 결론대로 슬롯을 늘리는 것보다 이쪽이 쌉니다.
- **클라이언트 개선을 실사용 RTT 에서 재야 합니다.** localhost 에서는 값이 드러나지
  않습니다 (8번 ⚠ 참고).
- **첨부는 아직 원본이 그대로 내려갑니다.** 한 글에 사진 3MB + 영상 26.8MB 가 실려
  있습니다. 업로드 시 썸네일 생성과 `fs.readFile` 대신 스트리밍이 다음 수순입니다.
- 2부의 남은 것(대시보드에 wait·exec·loop, 프록시 재측정)은 그대로입니다.


---
---

# 4부 — 목표를 정하고 다시 재다 (2026-09-11)

1~3부에는 **얼마나 빨라졌는가**가 있고 **얼마나 빨라야 하는가**가 없습니다.
기준선이 "20 VU", 한계 탐색이 "200 VU" 였는데, 그 200 VU 가 우리 서비스에서 몇
명인지는 아무도 정한 적이 없습니다. 그래서 8프로세스·풀 88 같은 구성이 필요한
것인지 과한 것인지 판단할 근거가 없었고, "천장 1,745 req/s" 같은 좋아 보이는
숫자가 목표와 얼마나 떨어져 있는지도 몰랐습니다.

이번에 목표가 생겼습니다 — **가입자 10,000명 · DAU 3,000명.**
4부는 그 숫자에서 다시 출발해, 기준선·시나리오·데이터·구성을 전부 다시 깝니다.

결론부터: **처리량은 이미 목표의 100배가 넘게 남아돌고, 진짜 병목은 바이트였습니다.**

---

## 10. 목표를 요청/초로 옮기기

`load-test/capacity-model.mjs` 가 이 계산의 정본입니다. 가정을 바꾸면 k6 시나리오와
문서가 같은 숫자를 따라오도록 한 곳에 모아 뒀습니다.

### 세션당 요청 — 셌습니다

프로덕션 빌드에 브라우저를 붙여 페이지마다 실제로 나가는 요청을 셌습니다.

| 화면 | 요청 | 무엇 |
|---|---:|---|
| 홈(지도·목록) | 3 | `auth/session` · `machines` · `arcades` |
| `/live` | 3 | `auth/session` · `machines` · `reports` **+ 30초마다 1** |
| 커뮤니티 | 3 | `auth/session` · `boards` · `posts` |
| 서열표 | 2 | `auth/session` · `tier` |
| 오락실 상세(열 때마다) | 2 | `reports` · `reviews` |
| 글 상세(열 때마다) | 1 | `posts/:id` |

한 세션을 "홈 → 오락실 2곳 → 커뮤니티 → 글 1개 → 서열표 → (20%는 /live)" 로 잡으면
**14.3 요청**입니다. 모델에는 안전하게 15 를 씁니다.

### 도출

| | 값 | 어떻게 |
|---|---:|---|
| 세션/일 | 4,500 | DAU 3,000 × 1.5 |
| API 요청/일 | 67,500 | 4,500 × 15 |
| 피크아워 세션 | 810 | 일일의 18% (평일 21~23시 집중) |
| **동시 사용자** | **54명** | 810 × (4분 / 60분) |
| **평시 피크** | **3.4 req/s** | 810 세션/h × 15 ÷ 3600 |
| **순간 버스트** | **10.1 req/s** | 피크 × 3 |
| 성장 헤드룸(×10) | 33.8 req/s | DAU 30,000 상당 |

### 이 숫자가 기존 측정과 만나는 지점

| | 값 | 목표 대비 |
|---|---:|---|
| 평시 피크 | 3.4 req/s | — |
| 1프로세스·풀 30 (3부 실측) | 333~338 req/s | **98배** |
| 8프로세스·풀 88 (2부 실측) | 550 req/s | **162배** |
| 포화 천장 (2부 실측) | 1,745 req/s | **513배** |

**8프로세스 구성은 목표의 162배입니다.** 2부에서 그 구성을 고른 근거는
"200 VU 에서 p95 가 12배 좋아진다" 였는데, 200 VU 는 우리 서비스로 치면
동시 사용자 54명의 **3.7배**이고, 그것도 think time 없이 몰아치는 조건이었습니다.

---

## 11. 기준선을 다시 깔았습니다 — `k6-dau.js`

기존 `k6-read.js` 는 두 가지가 목표와 안 맞습니다.

**① 닫힌 모델(VU)로는 목표 처리량을 물을 수 없습니다.** VU 를 고정하면 서버가
느려질 때 부하도 같이 느려집니다. "이만큼 들어올 때 견디는가" 를 물으려면 도착률을
고정해야 합니다 — `constant-arrival-rate`. 목표 rps 를 아는 지금에야 쓸 수 있는
방법입니다.

**② 한 반복이 6개 엔드포인트를 `http.batch` 로 동시에 때립니다.** 실제 사용자는
한 페이지에 머물다 다음으로 갑니다. 1부 '남은 것' 이 스스로 지적해 둔 문제이고,
이번에 고쳤습니다 — 한 반복 = 한 세션이고, 위에서 실측한 순서를 그대로 따라갑니다.

### SLO 도 다시

| | 기존 | 새것 | 왜 |
|---|---|---|---|
| p95 | < 500ms | **< 200ms** | 500ms 는 200 VU 포화를 전제한 값. 평시 피크에서는 너무 헐렁합니다 |
| p99 | 없음 | **< 500ms** | 꼬리를 봐야 "가끔 느리다" 를 잡습니다 |
| 실패율 | < 1% | **< 0.5%** | |
| 버려진 반복 | 없음 | **< 5** | 도착률을 못 채우면 측정 자체가 무효입니다 |

---

## 12. 빈 테이블 위의 기준선은 무효였습니다

1부 '남은 것' 에 이렇게 적혀 있었습니다 — *"기종 데이터가 비어 있습니다. …
데이터를 채우면 이 기준선은 무효가 됩니다."* 그 경고가 맞았고, **얼마나** 맞았는지를
이번에 쟀습니다.

`load-test/seed-scale.sql` 로 목표 규모(10,000 가입자 · DAU 3,000 · 1년치)를 만들어
별도 DB(`arcade_finder_scale`)에 넣었습니다. 개발 DB 는 건드리지 않습니다 — 스크립트
맨 위 가드가 DB 이름이 `_scale` 로 끝나지 않으면 거부합니다.

| | 개발 DB | 스케일 DB |
|---|---:|---:|
| `arcade_machines` | 4 | **2,821** |
| `arcade_cabinets` | 8 | **5,644** |
| `machine_reports` | 0 | **25,652** |
| `arcade_reviews` | 0 | **6,000** |
| `posts` / `post_comments` | 30 / 28 | **20,030 / 40,028** |
| `players` | 16 | **10,016** |
| `difficulty_votes` | 90 | **150,459** |
| DB 크기 | 13MB | **176MB** |

> 위 숫자는 **측정이 실제로 돌아간 그 DB** 의 건수입니다. `seed-scale.sql` 을 새로
> 돌리면 건수가 달라집니다 — `LIMIT 2 + (random()*4)::int` 류를 Postgres 가 쿼리당
> 한 번만 평가하기도 해서 배수가 흔들립니다(재현 확인: `arcade_machines` 2,821~3,759 ·
> `post_comments` 40,028~120,028). 자릿수는 맞으므로 부하 데이터로는 충분하지만,
> **두 측정을 비교할 때는 같은 DB 를 계속 쓰세요.**

### 같은 부하, 같은 구성, 데이터만 다르게 (평시 피크 · 1프로세스 · 풀 30)

| | 개발 DB | 스케일 DB | |
|---|---:|---:|---|
| p95 | 22.0ms | **152.0ms** | 6.9배 |
| p99 | 36.1ms | 188.4ms | 5.2배 |
| `/api/arcades` p95 | 41.1ms | **191.4ms** | 4.7배 |
| 세션당 수신 | 332.6KB | **1,285.0KB** | 3.9배 |
| 실패 | 0 | 0 | |

SLO(p95 < 200ms)는 통과합니다. 다만 **마진이 9배에서 1.3배로 줄었습니다.**
그리고 늘어난 것의 정체가 처리량이 아니라 **바이트**라는 게 드러납니다.

---

## 13. 진짜 병목은 바이트였습니다 — API 응답이 압축되지 않고 있었습니다

`/api/arcades` 하나가 스케일 DB 에서 **1.2MB** 입니다. 세션마다 한 번 나가니
하루 4,500 세션이면 **5.4GB/일 · 165GB/월** 입니다.

그런데 재 보니 그 1.2MB 가 **압축 없이** 나가고 있었습니다.

```
GET /                → Content-Encoding: gzip     ✓
GET /_next/.../x.js  → Content-Encoding: gzip     ✓
GET /api/arcades     → (헤더 없음)  1,224,603 B   ✗
```

Next 는 `next start` 에서 **렌더된 HTML 과 정적 파일만** 압축합니다
(`node_modules/next/dist/docs/.../compress.md`). 라우트 핸들러가 돌려주는 JSON 은
그 경로를 타지 않습니다. localhost 에서 k6 로 재면 절대 안 보이는 종류의 문제입니다 —
바이트는 공짜고 지연만 재고 있었으니까요.

### `lib/http.ts`

`NextResponse.json` 자리에 넣는 `json(request, data)` 하나입니다. 무거운 GET 7곳에
적용했습니다(`arcades` · `arcades/:id` · `arcades/:id/reports` · `posts` ·
`posts/:id` · `tier` · `reports`).

- **brotli q4 우선, gzip level 6 폴백.** 재서 골랐습니다 — 299KB 기준 br q4 는
  46.3KB(2.02ms)로 gzip level 9(47.2KB · 4.55ms)보다 작고 빠릅니다.
- **동기 압축을 쓰지 않습니다.** 2부가 이 앱의 병목을 이벤트 루프 지연으로 짚었는데,
  `gzipSync` 는 1.2MB 에 루프를 10ms 넘게 잡습니다. 바이트를 아끼려다 병목을 키우는
  꼴이라 비동기 zlib(libuv 스레드풀)을 씁니다.
- **1,400바이트 미만은 건드리지 않습니다.** MTU 안에 들어가면 왕복이 안 줄고 CPU 만 씁니다.
- **`Vary: Accept-Encoding` 을 항상 붙입니다.** 없으면 중간 캐시가 압축 본문을
  압축 모르는 클라이언트에게 줍니다.
- **프록시를 앞에 두면 `API_COMPRESSION=off`.** nginx·caddy 가 맡는 게 정석이고,
  그때 이 계층은 중복입니다.

### 측정 — 같은 스케일 DB, 두 서버를 동시에 띄우고

| 엔드포인트 | 압축 전 | 압축 후 | 절감 |
|---|---:|---:|---:|
| `/api/arcades` | 1,224,603 B | **90,434 B** | **−92.6%** |
| `/api/reports?limit=80` | 26,926 B | 3,393 B | −87.4% |
| `/api/posts?limit=20` | 9,649 B | 1,154 B | −88.0% |
| `/api/tier` | 7,616 B | 1,476 B | −80.6% |

풀어서 md5 를 비교하면 압축 전 서버의 응답과 **바이트 단위로 같습니다.**

### 월 대역폭 (DAU 3,000 · 4,500 세션/일)

부하 시나리오에서 실제로 받은 바이트입니다(추정이 아니라 `data_received` 실측).

| | 세션당 | 일 (4,500 세션) | 월 |
|---|---:|---:|---:|
| 압축 전 | 1,286.1 KB | 5.5 GB | **165 GB** |
| 압축 후 | **106.8 KB** | 0.46 GB | **14 GB** |

---

## 14. 만료 제보 정리가 읽기마다 테이블을 통째로 훑고 있었습니다

`purgeExpiredQueueReports()` 는 스케줄러가 없어서 **읽을 때마다** 돕니다 —
`/live` 피드(30초 폴링), 오락실 상세, 제보 등록. 즉 **읽기 요청 하나가 DELETE 하나**입니다.
제보가 0건이던 개발 DB 에서는 공짜였습니다.

25,680건을 채우고 재면:

```
Delete on machine_reports  (actual time=3.306..3.308 rows=0)
  -> Seq Scan on machine_reports  (rows=291, Rows Removed by Filter: 25,389)
     Buffers: shared hit=398        Execution Time: 3.809 ms
```

**지울 것이 하나도 없는 평상시에도 전체를 훑습니다.** 대기 제보는 4시간만 살아서
늘 전체의 1% 남짓인데 나머지 99% 를 매번 읽고 버립니다. 기존 인덱스로는 못 받습니다 —
`(created_at DESC)` 는 오래된 쪽 전부가 조건에 걸리고, `(arcade_id, machine_id, kind, …)`
는 선두 컬럼이 `arcade_id` 라 전 오락실을 훑는 이 쿼리가 못 탑니다.

**`migrate-053-queue-purge-index.sql`** — `WHERE kind = 'queue'` 부분 인덱스 하나.

| | 전 | 후 |
|---|---:|---:|
| 실행 시간 | 3.809 ms | **0.458 ms** (−88%) |
| 버퍼 | 398 | **5** (−99%) |
| 계획 | Seq Scan | Bitmap Index Scan |

> **읽기 경로에서 DELETE 를 떼어내는 것(호출 간격 제한)은 하지 않았습니다.**
> 인덱스를 넣은 뒤 0.46ms 라 목표 부하(3.4 req/s)에서 얻을 것이 사실상 없고,
> 프로세스마다 타이머 상태가 생깁니다. 읽기 rps 가 지금의 수십 배가 되면 그때
> 꺼내 쓸 수 있는 카드로 남겨 둡니다 — **지금 넣는 것은 4부가 고치려는 그
> 과최적화 그 자체입니다.**

---

## 측정 (4부)

프로덕션 빌드 · PostgreSQL 18 · **스케일 DB**(§12) · 1프로세스 · 풀 30.
BEFORE(압축 없음)와 AFTER(압축+인덱스)를 **각자 포트에 동시에 띄우고** 번갈아 쟀습니다
(2부의 측정 방법 — 순차로 재면 배경 드리프트가 효과를 덮습니다).

### 평시 피크 (DAU 3,000 · 2.7 req/s)

| | 개발 DB | 스케일 DB | 스케일 + 압축 |
|---|---:|---:|---:|
| 처리량 | 2.7/s | 2.7/s | 2.7/s |
| 중앙값 | 5.1ms | 6.1ms | 5.6ms |
| p95 | **22.0ms** | 152.0ms | 155.6ms |
| p99 | 36.1ms | 188.4ms | 189.4ms |
| 실패 | 0 | 0 | 0 |
| **세션당 수신** | 332.6KB | 1,285.0KB | — |

### 성장 헤드룸 ×10 (DAU 30,000 상당 · 25.4 req/s)

| | 압축 전 | 압축 후 | |
|---|---:|---:|---|
| 처리량 | 25.5/s | 25.3/s | 같음 |
| 중앙값 | 4.9ms | 6.0ms | +1.1ms |
| p95 | 144.3ms | 159.1ms | **+10%** |
| p99 | 182.6ms | 197.7ms | +8% |
| 실패 | 0 | 0 | |
| **세션당 수신** | **1,286.1KB** | **106.8KB** | **−91.7%** |
| 3분간 총 수신 | 496.1MB | 42.1MB | |

**처리량 10배(2.7 → 25.4 req/s)에 p95 가 오히려 조금 내려갑니다.** 대기가 생기는
구간이 아니라는 뜻이고, p95 를 만드는 것은 동시성이 아니라 `/api/arcades` 응답
그 자체(191~207ms)입니다.

### ⚠ localhost 에서 압축은 손해로 보입니다 — 실사용에서는 반대입니다

위 표에서 압축 후 p95 가 **10% 나빠졌습니다.** 서버가 브로틀리에 CPU 를 쓰는데
localhost 에서는 1.2MB 를 보내는 값이 사실상 0 이라, 비용만 보이고 이득이 안 보입니다.
실제 회선을 넣으면 부호가 뒤집힙니다.

| 회선 | 1,286KB | 107KB | 차이 |
|---|---:|---:|---:|
| LTE 20Mbps | 514ms | 43ms | **−471ms** |
| 5G 100Mbps | 103ms | 9ms | −94ms |
| localhost | ~0 | ~0 | +15ms (압축 CPU) |

3부 '남은 것' 의 *"클라이언트 개선을 실사용 RTT 에서 재야 합니다"* 가 그대로
적용되는 자리입니다. **localhost 수치만 보고 압축을 되돌리지 마세요.**

### 검증

`npm run typecheck` 통과. `tests/http-compression.test.ts` 9건 추가 — 헤더가 아니라
**풀었을 때 원본과 같은가**를 봅니다(압축은 조용히 틀리는 종류의 최적화입니다).
돌아가는 서버에서도 확인했습니다.

```
curl --compressed :3300/api/posts?limit=20 | md5  ==  curl :3200/api/posts?limit=20 | md5
```

압축 전 서버와 **바이트 단위로 같습니다.**

---

## 15. 구성을 다시 정합니다 — 8프로세스 · 풀 88 은 목표의 162배였습니다

2부는 "8프로세스 · 프로세스당 풀 11(합 88)" 로 끝났습니다. 근거는 200 VU 에서 p95 가
1.3초에서 90ms 로 좋아진다는 것이었고 그 자체는 맞습니다. 다만 **200 VU 가 우리
목표의 몇 배인지를 몰랐습니다** — 동시 사용자 54명의 3.7배이고, think time 도 없는
조건입니다.

### 풀 상한은 병목이 아닙니다 — 도달조차 하지 않습니다

목표의 10배 부하(25 req/s)를 거는 동안 `pg_stat_activity` 를 2~3초 간격으로 떴습니다.

| 서버 | 상한 | 실제 열린 커넥션 | 그중 활성 |
|---|---:|---:|---:|
| 3300 | 30 | **4~5** | 0~1 |
| 3400 | 10 | **3~5** | 0 |

상한이 30 인데 실제로 열리는 것은 4~5개입니다. 풀은 필요한 만큼만 엽니다.

### 풀 30 대 풀 10 — 같은 빌드를 두 포트에 동시에 띄우고 번갈아 2라운드

| | r1 | r2 | 중앙 |
|---|---:|---:|---:|
| **풀 30** 처리량 | 25.4/s | 25.4/s | 25.4/s |
| **풀 30** p95 | 165.4ms | 150.6ms | **158.0ms** |
| **풀 10** 처리량 | 25.2/s | 25.5/s | 25.4/s |
| **풀 10** p95 | 155.1ms | 159.0ms | **157.1ms** |

**차이가 없습니다** (p95 −0.6%, 라운드 간 변동 ±5% 안). 3부가 "30~50 사이에 차이가
없다" 고 결론냈는데, 목표 부하에서는 **10~30 사이에도 없습니다.**

### 그래서 무엇을 쓸 것인가

| | 지금까지의 계획 (2부) | **목표 기준 권고** | 왜 |
|---|---|---|---|
| 프로세스 | 8 | **2** | 1개가 목표의 10배를 p95 159ms 로 받습니다. 2개인 이유는 속도가 아니라 **무중단 배포와 장애 격리**입니다 |
| `PG_POOL_MAX` | 11 (합 88) | **10** (합 20) | 실측 사용량이 4~5. 2배 여유 |
| Postgres 피크 RAM | 약 2,024MB | **약 460MB** | 접속당 23MB(3부 실측) × 20 |
| `max_connections=100` 안에서 | 인스턴스 8개로 꽉 참 | **인스턴스 9개까지 여유** | 20 ÷ 100 |

`lib/db.ts` 의 기본값은 **30 그대로 둡니다.** 바꿔서 얻는 것이 없고(위 표), 상한은
할당이 아니라 천장이라 놔둬도 비용이 0 입니다. 대신 **배포 시 인스턴스당 10 을 주라**
는 것을 기록해 둡니다 — 바뀐 것은 코드가 아니라 용량 산정 규칙입니다.

> ⚠ **2부의 "8 × 11 = 88 이 이 장비의 상한선" 은 더 이상 제약이 아닙니다.** 그 계산은
> `max_connections` 를 꽉 채우는 구성을 전제로 했는데, 목표 부하에서는 그 구성 자체가
> 필요 없습니다.

### 권고 구성을 실제로 만들었습니다 — `npm run start:cluster`

권고만 적어 두면 아무도 안 씁니다. 2 × 10 이 **돌아가는 구성**이 되도록
`scripts/start-cluster.mjs` 를 넣었습니다.

- 인스턴스 2개를 내부 포트(3001·3002)에 `PG_POOL_MAX=10` 으로 띄우고,
  공개 포트(3000)에 얇은 라운드로빈 프록시를 둡니다.
- `/api/health` 200 을 받아야 트래픽을 보냅니다 — 포트 바인딩만 보면 DB 가 안 붙은
  인스턴스에도 요청이 갑니다.
- 인스턴스가 죽으면 프록시가 건너뛰고, 지수 백오프로 다시 띄웁니다.

**왜 `next start` 를 그대로 두는가** — `node:cluster` + 커스텀 서버로 포트를 공유하면
프록시 홉이 없어 더 깔끔합니다. 그런데 4부의 모든 수치가 `next start` 로 잰 것이라,
갈아타면 방금 세운 기준선이 그대로 무효가 됩니다.

**왜 노드 프록시가 2부 경고와 충돌하지 않는가** — 2부는 "노드 프록시는 단일 스레드라
천장이 프록시로 옮겨온다" 고 적었고, **1,700 req/s 를 재던 맥락에서는 맞습니다.**
목표는 3.4 req/s(버스트 10)라 두 자릿수 아래입니다. 수백 req/s 에 가까워지면 nginx·caddy
로 옮기고 `API_COMPRESSION=off` 로 압축도 함께 넘기세요.

### "2개인 이유" 를 실제로 확인했습니다

주장으로 두지 않고 죽여 봤습니다. 0.25초 간격으로 `/api/health` 를 때리는 중에
3001 인스턴스를 `taskkill /F` 로 강제 종료했습니다.

```
200 200 200 200 [3001 강제 종료] 200 200 200 … 200      ← 48/48 전부 200
[cluster] :3001 종료 (code=1 signal=null) — 1000ms 뒤 재기동
[cluster] :3001 기동 (PG_POOL_MAX=10)
[cluster] :3001 준비됨
```

**사용자 쪽 영향 0.** 1프로세스 구성이었다면 이 순간 사이트 전체가 내려갑니다.
이것이 2개를 쓰는 유일한 이유이고, 이제 근거가 있습니다.

### 재측정 — 그리고 "2개는 속도 때문이 아니다" 를 정정합니다

권고 구성을 띄워 놓고 같은 시나리오를 다시 쟀습니다 (스케일 DB · 압축 · 인덱스 동일).

| 성장 ×10 (25.4 req/s) | 1프로세스 · 풀 30 | **2프로세스 · 풀 10 · 프록시** | |
|---|---:|---:|---|
| 처리량 | 25.3/s | 25.4/s | 같음 |
| 중앙값 | 6.0ms | **4.2ms** | −30% |
| p95 | 159.1ms | **98.9ms** | **−38%** |
| p99 | 197.7ms | **114.9ms** | −42% |
| 최대 | 241.3ms | **163.1ms** | −32% |
| `/api/arcades` p95 | 207.4ms | **130.0ms** | −37% |
| 세션당 수신 | 106.8KB | 107.5KB | 같음 ✓ |
| 실패 | 0 | 0 | |

평시 피크에서도 같은 방향입니다 — p95 155.6 → **101.7ms**, p99 189.4 → **123.5ms**.

> **위에서 "2개인 이유는 속도가 아니라 가용성" 이라고 썼는데, 그건 절반만 맞았습니다.**
> 가용성이 2개를 쓰는 *이유*인 것은 그대로이지만, 실제로는 **p95 가 38% 좋아집니다.**
>
> 왜인지는 분명합니다. 13절에서 응답 압축을 넣으면서 **요청당 가장 무거운 일이
> 브로틀리 압축(1.2MB)** 이 됐습니다. 그건 순수 CPU 라 단일 이벤트 루프에서는
> 다른 요청을 막지만, 프로세스가 둘이면 겹쳐서 돕니다. `/api/arcades` p95 가
> 207 → 130ms 로 줄어든 것이 그 자리입니다.
>
> 압축 이전 코드였다면 이 차이는 안 나왔을 것입니다 — **13절이 15절의 결론을
> 바꾼 셈**이고, 순서를 뒤집어 쟀다면 놓쳤을 것입니다.

프록시 홉이 지연을 늘리지도 않았습니다(오히려 줄었습니다). 세션당 바이트가 같으므로
프록시가 압축을 훼손하지도 않습니다.

### 천장은 못 찾았습니다 — 부하 생성기가 먼저 바닥났습니다

도착률을 계단으로 올려(세션 1 → 20/s) 1프로세스가 어디서 무너지는지 보려 했습니다.

| | 값 |
|---|---:|
| 달성 처리량 | **69.7 req/s** (목표의 **20배**) |
| p95 | 159.1ms |
| p99 | 207.9ms |
| 최대 | 479.7ms |
| **실패** | **0%** |
| 버려진 반복 | **214** ← 임계값 위반 |

**임계값 하나가 깨졌는데, 깨진 쪽은 서버가 아닙니다.** `dropped_iterations` 는 k6 가
새 세션을 시작할 VU 를 못 구했을 때 늘어납니다. 서버 쪽 지표는 전부 멀쩡합니다 —
실패 0%, p95 159.1ms 로 성장 시나리오(157ms)와 **사실상 같습니다.** 서버가 힘들었다면
p95 가 올라갔어야 합니다.

즉 이 측정이 말해 주는 것은 "1프로세스의 천장은 X" 가 아니라
**"최소 69.7 req/s, 목표의 20배까지는 티도 안 난다"** 입니다.

> **진짜 천장은 안 찾기로 했습니다.** `maxVUs` 를 올리고 부하를 분산하면 찾을 수
> 있지만, 그건 4부가 고치려는 바로 그 일 — **목표 없이 큰 숫자를 쫓는 것** — 입니다.
> 목표가 3.4 req/s 인데 20배에서 멀쩡하다는 것으로 용량 판단은 끝납니다.
> 정말 필요해지면 `k6-dau.js` 의 `ceiling` 시나리오에서 `maxVUs` 를 올리세요.

---

## 16. 남은 것 (4부) — 목표 기준으로 다시 줄 세운 것

목표가 정해지니 우선순위가 바뀝니다. **처리량 항목은 전부 내려가고, 바이트·저장소
항목이 올라옵니다.**

### 1순위 — 첨부. 이 서비스에서 가장 큰 것은 DB 가 아니라 파일입니다

| | 크기 |
|---|---:|
| 목표 규모 DB 전체 (1년치) | **176MB** |
| 지금 첨부 4개 (글 3개분) | **34MB** |
| 그중 영상 하나 | **26.8MB** |

글 20,000개 중 10%가 첨부를 달면 **연 22GB** — DB 의 125배입니다. 게다가:

- **구간 요청(Range)도 파일 전체를 메모리에 올립니다.** `app/api/uploads/[id]` 는
  `read()` 로 통째로 읽고 `subarray` 로 자릅니다. 26.8MB 영상 하나를 되감으면
  **요청마다 28.6MB** 가 잡힙니다(실측). 동시에 10명이 보면 286MB 입니다.
- 원본을 그대로 내려보냅니다 — 썸네일 **파일**이 없습니다. 상세 화면은 사진 3MB 가
  그대로 뜹니다. (목록은 `next/image` 가 요청 시점에 줄여 캐시하도록 막아 뒀습니다 —
  79KB PNG 가 280바이트 webp 로 나갔습니다. 저장 방식은 그대로라 상세는 해당 없음)

**해야 할 것**: 업로드 시 썸네일 생성 · `fs.readFile` 대신 `createReadStream` ·
업로드 크기 상한. 4부에서 압축으로 API 바이트를 1/12 로 줄였는데, 첨부는 손도
안 댄 상태이고 자릿수가 하나 더 큽니다.

### 2순위 — `/api/arcades` 를 목록과 상세로 쪼갤 것인가

압축 후에도 이 응답 하나가 세션 바이트의 대부분이고 p95 를 혼자 만듭니다(191~207ms).
구성은 이렇습니다 (목표 규모 데이터 · 939곳).

| | 무압축 | brotli |
|---|---:|---:|
| 지금 그대로 | 1,196KB | **88KB** |
| `cabinets` 빼면 | 598KB | 53KB |
| 지도에 필요한 것만(id·이름·좌표·기종 id) | 94KB | 22KB |

`machines` 배열이 전체의 75%, 그중 `cabinets` 가 47% 입니다. 다만 **지금은 상세
패널이 목록에서 받은 객체를 그대로 씁니다** — 빼려면 상세를 열 때 `/api/arcades/:id`
를 한 번 더 부르도록 클라이언트를 고쳐야 하고, 로딩 상태가 하나 생깁니다.
압축으로 88KB 까지 내려온 지금, 53KB 를 더 줄이자고 그 설계를 바꿀지는 **판단이
필요한 자리**입니다 — 숫자는 위에 있습니다. 월 대역폭으로는 21GB → 12GB 입니다.

### ~~3순위 — 쓰기 부하를 한 번도 안 쟀습니다~~ → **17절에서 쟀습니다**

1~4부 전부 읽기만 쟀습니다. 목표 규모에서 쓰기는 이 정도로 추정됩니다.

| | 하루 |
|---|---:|
| 제보 (DAU 의 5%) | 150 |
| 글 | 55 |
| 댓글 | 165 |
| 투표·클리어 기록 | 수백 |

절대량은 작지만 경로가 안 재져 있습니다. 특히 **제보 등록은 보유 기종 자동 반영
(`applyPresence`)까지 도는 가장 무거운 쓰기**이고, 대회 직후처럼 한 오락실에 제보가
몰리는 패턴이 실제로 있습니다.

### 4순위 — `posts` 가 본문을 두 번 저장합니다

`body`(평문)와 `body_doc`(JSONB)에 같은 내용이 들어갑니다. 목표 규모에서 `posts`
테이블이 **92MB** 로 DB 전체(176MB)의 절반이고, 그 대부분이 이 중복입니다.
평문은 검색(trigram 인덱스)에 쓰이므로 그냥 지울 수는 없고, 정하고 가야 합니다.

### 그대로 남는 것

- `/api/tier` 가 아직 요청당 2.0 쿼리 (3부에서 넘어옴). **목표 부하에서는 급하지
  않습니다** — 8ms 짜리이고 여유가 100배입니다.
- 대시보드에 `wait`·`exec`·`loop` 를 나란히 (2부).
- 프록시를 앞에 두고 재측정 (2부). 프록시가 생기면 `API_COMPRESSION=off` 로 압축을
  넘기는 것도 같이 정하세요.
- 접근성 P1(목록 키보드화) → P2(라이브 리전). 성능과 별개로 남아 있습니다.

### 재발굴 금지 — 4부에서 추가

- **풀 상한은 병목이 아닙니다.** 목표의 10배 부하에서 열린 커넥션이 4~5개(활성 0~1)
  입니다. 상한 30 에 도달조차 하지 않습니다. 1부의 "풀 상한이 관건인 구간" 은
  200 VU 를 전제로 한 이야기이고, 그 구간은 우리 목표의 37배 위에 있습니다.
- **프로세스를 8개로 늘리는 것은 처리량 문제가 아닙니다.** 1프로세스가 목표의 10배를
  p95 159ms 로 받아냅니다. 여러 개를 띄운다면 이유는 **가용성**(무중단 배포·프로세스
  하나가 죽어도 서비스가 산다)이지 속도가 아닙니다.
- **localhost 지연만 보고 압축을 되돌리지 마세요.** 여기서는 압축이 p95 를 10%
  나쁘게 만듭니다. 실제 회선에서는 세션당 −471ms(LTE 20Mbps)입니다.

---

## 17. 쓰기를 쟀습니다 (2026-09-11)

1~4부는 전부 읽기만 쟀습니다. 16절이 남겨 둔 항목이고, 특히 **제보 등록은 트랜잭션
안에서 보유 기종 자동 반영까지 도는 이 앱에서 가장 무거운 쓰기**입니다.

### 먼저 — 쓰기는 양이 문제가 아닙니다

`capacity-model.mjs` 에 쓰기 믹스를 넣고 뽑았습니다.

| | 하루 |
|---|---:|
| 제보 | 210 |
| 난이도 투표 · 클리어 기록 | 400 |
| 댓글 | 165 |
| 글 추천 토글 | 250 |
| 즐겨찾기 토글 | 150 |
| 글쓰기 | 55 |
| 리뷰 | 15 |
| **합계** | **1,245** |

**피크 0.062 writes/s · 읽기:쓰기 = 54 : 1.** 그래서 "몇 건 받아내나" 는 물을 가치가
없습니다. 대신 둘을 물었습니다 — **건당 왕복 수**와 **한 곳에 몰릴 때**.

### 요청 1건당 DB 트랜잭션 — 읽기 1.0 대 쓰기 4.2~6.8

`pg_stat_database.xact_commit` 증가분입니다(1부와 같은 방법).

| 요청 | 트랜잭션/건 |
|---|---:|
| `GET /api/arcades/:id` | **1.0** |
| `POST` 글 추천 토글 | 3.1 |
| `POST` 제보 (대기) | **4.2** |
| `POST` 제보 (컨디션) | **6.8** |

읽기는 3부에서 1회까지 접었는데 쓰기 경로는 손댄 적이 없어서 그대로입니다.
제보 한 건이 도는 것: 오락실 존재 확인 → 설정 조회 → 만료 제보 정리(DELETE) →
보유 기종 확인 → (컨디션이면 기체 확인) → 트랜잭션(INSERT + applyPresence + 피드 재조회)
→ **응답용 오락실 재조회**.

> ⚠ **처음엔 10건으로 나왔습니다.** 클러스터의 헬스체크가 2초마다 `/api/health`
> (`SELECT 1`)를 돌려서 그게 같이 세어진 것입니다. 헬스체크가 없는 단일 서버로
> 옮기고 기저(10초에 4건)를 확인한 뒤 다시 잰 값이 위 표입니다.
> **`xact_commit` 으로 재려면 재는 동안 그 DB 에 다른 트래픽이 없어야 합니다.**

### 쓰기 믹스 — 피크의 32배로

피크(0.062/s)로 돌리면 2분에 7건이라 분포가 안 나옵니다. **포화가 아니라 건당 값을
보는 측정**이라 2 writes/s 로 돌렸습니다.

| | 값 |
|---|---:|
| 처리량 | 2.00 writes/s (243건) |
| 중앙값 | 14.2ms |
| p95 | 24.7ms |
| p99 | 27.4ms |
| 최대 | 1,508ms |
| 실패 | **0%** |

| 엔드포인트 | p95 |
|---|---:|
| 제보 (대기) | 24.9ms |
| 제보 (컨디션) | 24.1ms |
| 제보 (있어요) | 22.2ms |
| 리뷰 | 24.9ms |
| 글 추천 토글 | 20.8ms |

> ⚠ **즐겨찾기는 이 믹스에 없습니다.** 이 앱에서 세션을 근거로 쓰는 유일한 경로인데
> 하네스 문제를 못 풀어 뺐습니다. VU 1개로는 정상입니다 — 가입 201 → 쿠키 보관 →
> `/api/auth/session` 이 user 반환 → `POST /api/favorites` **200**. 그런데 동시 VU 로는
> 일부가 401 을 받습니다. 가입 잠김(429)도 아니고(가입은 계속 201, 계정 81개 생성됨),
> 쿠키 Secure 속성 문제도 아닙니다(k6 가 정상 보관·전송하는 것을 확인). **원인을
> 못 찾아 재는 척하지 않고 제외했습니다.** 하루 150건 · INSERT 한 번이라 믹스의
> 성격은 안 바뀝니다.
>
> **→ 원인은 나중에 나왔습니다. 이 절 끝의 "뒤늦게 찾은 원인" 을 보세요.**
>
> 가는 길에 틀린 진단을 두 번 했고 둘 다 되돌렸습니다 — ① "실패율 30%" 는 서버가
> 아니라 k6 에 기대 상태코드를 안 알려 준 것(409·401 을 실패로 셈) ② "Secure 쿠키라
> 평문 HTTP 에서 세션이 안 선다" 는 **틀렸습니다**(k6 는 보관·전송합니다).

### 쏠림 — 대회 직후 한 오락실에 몰릴 때

모델은 30명/2분 = 0.25 writes/s 인데, 그 **360배**로 밀어 봤습니다.
20 VU 가 전부 같은 오락실·같은 기종에 있어요/없어졌어요를 던집니다.

| | 값 |
|---|---:|
| 처리량 | **90.2 writes/s** |
| 중앙값 | 15.0ms |
| p95 | 23.1ms |
| p99 | 28.7ms |
| **최대** | **2,162.9ms** |
| 실패 | **0%** |

**8,256건을 한 곳에 쏟아부어도 실패가 0 이고 정합성도 안 깨집니다** —
`arcade_machines` 중복 0, 고아 기체 0. `applyPresence` 의 `ON CONFLICT DO NOTHING`
이 제 몫을 합니다.

p95 는 23ms 인데 **최대가 2.16초**인 것이 이 측정의 유일한 그림자입니다. 같은 행을
동시에 INSERT/DELETE 하는 트랜잭션들이 서로를 기다린 자리입니다. 목표(0.25/s)의
360배에서 나온 값이라 지금 손댈 이유는 없습니다.

### 여기서 나온 것 — '없어졌어요' 합의가 컨디션 이력을 지웁니다

부하가 아니라 **데이터 수명**에 관한 것이고, 쓰기를 돌려 보지 않으면 안 보입니다.

```
arcade_cabinets (arcade_id, machine_id) → arcade_machines   ON DELETE CASCADE
machine_reports.cabinet_id              → arcade_cabinets   ON DELETE SET NULL
```

서로 다른 2명이 '없어졌어요' 를 올리면 `applyPresence` 가 `arcade_machines` 행을
지우고, 그 연쇄로 **기체가 사라지고 그 기체를 가리키던 컨디션 제보가 전부
`cabinet_id = NULL`** 이 됩니다. 기종이 다시 등록돼도 기체는 **새 id** 로 만들어지므로
옛 이력은 영영 안 붙습니다.

쏠림 측정에서 실제로 일어났습니다 — 대상 오락실의 기체 id 가 **17273 → 23035** 로
바뀌었고, 그걸 가리키던 컨디션 제보는 `cabinet_id` 가 비었습니다.

**버그로 보지 않습니다.** SET NULL 이 없으면 새 기체에 남의 이력이 잘못 붙습니다 —
지금 동작이 더 안전한 쪽입니다. 다만 두 가지는 기록해 둡니다.

- **2명이면 그 기종의 컨디션 이력이 조용히 사라집니다.** §8 R2(허위 제보)에 걸리는
  자리이고, 임계값 2 가 낮다는 근거로 쓸 수 있습니다.
- 화면은 이 일이 일어났다고 말하지 않습니다.

### 여기서 또 나온 것 — `ADMIN_PASSWORD` 가 없으면 아무도 로그인 못 합니다

성능과 무관하지만 쓰기를 재려다 걸렸습니다. 즐겨찾기가 이 앱에서 **세션을 근거로
쓰는 유일한 경로**라 k6 에서 로그인을 붙였더니 전부 503 이었습니다.

```
POST /api/auth/login  →  503  {"error":"ADMIN_PASSWORD 가 설정되지 않아 로그인할 수 없습니다"}
```

`app/api/auth/login/route.ts` 가 **아이디를 보기 전에** 이렇게 막습니다.

```ts
if (!configuredAdminPassword()) {
  return NextResponse.json({ error: 'ADMIN_PASSWORD 가 …' }, { status: 503 });
}
await ensureAdminAccount();
const user = await authenticate(parsed.data.nickname, parsed.data.password);
```

라우트가 **관리자 전용이던 시절의 가드**입니다(주석도 아직 "관리자 로그인"). 그 뒤에
일반 사용자 로그인(`authenticate`)이 같은 라우트에 얹혔는데 가드는 그대로 남아,
`ADMIN_PASSWORD` 가 없는 환경에서는 **일반 가입자도 로그인할 수 없습니다.**

눈에 잘 안 띄는 이유는 **회원가입이 세션 쿠키를 직접 심기 때문**입니다
(`app/api/auth/signup/route.ts` → `setSessionCookie`). 가입 직후에는 멀쩡히 로그인된
상태라, 로그아웃하거나 쿠키가 만료된 **뒤에야** 못 들어오는 걸 알게 됩니다.
OAuth 3종은 이 라우트를 안 타므로 영향 없습니다.

**고치지 않았습니다** — 이번 작업 범위(쓰기 부하 측정) 밖이고, 인증 분기는 따로
봐야 합니다. 측정은 가입으로 세션을 얻어 우회했습니다(`k6-write.js` 의 `ensureSession`).
§8 리스크 목록에 올려 둡니다.

### 결론 — 쓰기 경로는 지금 손대지 않습니다

건당 4~7 왕복은 읽기(1)에 비하면 크지만, **피크가 0.062 writes/s 라 초당 0.4 쿼리**
입니다. 줄일 자리는 분명히 보입니다(설정 조회 캐시 · 응답용 오락실 재조회 제거 ·
쓰기 경로의 만료 정리 생략). 셋 다 쉽고, **셋 다 지금 얻는 것이 없습니다.**
쓰기가 지금의 수백 배가 되면 그때 꺼내 쓸 목록으로 남깁니다 —
14절에서 만료 정리 간격 제한을 안 넣은 것과 같은 이유입니다.

### 재현

```bash
DATABASE_URL="...arcade_finder_scale" npm run start:cluster
```

```bash
k6 run -q -e BASE_URL=http://localhost:3000 -e SCENARIO=hotspot load-test/k6-write.js
```

⚠ **스케일 DB 에만 거세요 — 이 스크립트는 진짜로 씁니다.** 잰 뒤에는 덤프에서
되돌리세요(쏠림 한 번에 제보 8,000건이 한 오락실에 쌓입니다).

### 뒤늦게 찾은 원인 — 즐겨찾기 401 은 k6 의 쿠키 항아리였습니다

R1(신원을 세션으로)을 병합하면서 다시 봤습니다. **모든 쓰기가 세션을 요구하게 됐으니
더는 피해 갈 수 없었습니다.**

원인은 서버가 아니었습니다 — **k6 의 쿠키 항아리는 반복(iteration)마다 초기화됩니다.**
하네스는 가입을 첫 반복에서 한 번만 하고 "세션을 세웠다" 는 모듈 변수를 들고 있었으니,
**2회차부터는 쿠키 없이** 던지고 있었습니다. VU 1개 × 반복 1회로 확인할 때는 늘
성공하고, 반복이 도는 순간 대부분이 401 이 됩니다 — "동시 VU 면 일부가" 로 보인 것은
도착률 기반 실행기(constant-arrival-rate)가 VU 를 돌려쓰기 때문이었습니다.

쿠키 문자열을 직접 들고 다니게 고쳤습니다(`authed()`).

| | 고치기 전 | 고친 뒤 |
|---|---:|---:|
| 실패율 | 43.2% | **0.000%** |
| 거절된 쓰기 | 70건 | **0건** |

즐겨찾기가 믹스로 돌아왔고(p95 13.3ms), 쏠림에서 서로 다른 계정 21개가 임계값을
채워 `arcade_machines` 가 실제로 뒤집히는 것까지 확인했습니다. 함께 고친 것:

- 글 추천이 `POST` 토글에서 **`PUT`/`DELETE`** 로 바뀌어 있었는데(18절) 하네스는
  아직 `POST` 를 보내고 있었습니다 — 측정이 조용히 405 를 재고 있을 뻔했습니다.
- 요약의 "엔드포인트별 p95" 가 **안 돈 경로를 `0.0ms` 로** 찍고 있었습니다. 가장 빠른
  엔드포인트처럼 읽힙니다. 표본 0 은 빼고, 대신 건수를 함께 적습니다.

그리고 앞에서 "틀렸다" 고 적은 Secure 쿠키 이야기는 **그대로 틀린 채로 맞습니다** —
k6 는 `Secure` 를 무시하고 평문 HTTP 로도 보냅니다(운영 빌드 클러스터로 재확인).
브라우저는 안 보냅니다. 하네스가 통과하는 것과 브라우저에서 되는 것은 다릅니다.

---

## 18. 쿼리 전수 점검 — 하나가 나머지를 다 합친 것보다 무거웠습니다 (2026-09-11)

"전체적인 쿼리 최적화" 요청으로 시작했지만, 왕복 수를 더 줄이는 쪽은 이미 **안 하기로
판단한** 항목입니다(14·17절). 그래서 **아직 안 해 본 질문**을 골랐습니다 —
*데이터가 커졌을 때 계획이 무너지는 쿼리가 있는가.*

### 찾는 방법 — 눈으로 읽지 않고 통계로

`pg_stat_statements` 는 `shared_preload_libraries` 에 없어서(재시작 필요) 쓰지 않았습니다.
대신 스케일 DB 에서 `pg_stat_reset()` → 읽기 경로 20종을 3회씩 타격 → `pg_stat_user_tables`
의 순차 스캔 카운터를 읽었습니다. **어느 테이블이 실제로 훑히는지**가 근거입니다.

건강했던 것들(여기는 손대지 마세요):

| | 실측 |
|---|---|
| 게시글 검색 `ILIKE '%…%'` | **1.0ms** — trigram GIN(042)이 제 몫을 합니다 |
| 서열표 보드 | **0.67ms · 77버퍼** — `charts` 에 `vote_count`·`avg_vote`·`tier_code` 가 미리 계산돼 있어 15만 투표를 읽기 시점에 집계하지 않습니다 |
| `posts` 전체 스캔 2회 | 총계(COUNT)입니다. 무필터 COUNT 에 지름길은 없고 5분 캐시가 이미 있습니다 |
| `report_settings` 247회 | 누적값이고 요청당 1회입니다(측정으로 확인) |

> ⚠ **시드 데이터가 만든 착시 하나.** 처음에 `posts` 전체 스캔을 보고 검색 인덱스를
> 의심했는데, 2만 글 제목이 전부 `loadtest post title N` 이라 `q=loadtest` 가 **전부
> 매칭**되는 탓이었습니다. 선택적인 검색어로 다시 재니 인덱스를 정상으로 탑니다.
> 합성 데이터로 재는 동안에는 이런 착시를 늘 의심해야 합니다.

### 찾은 것 — `/api/arcades` 한 번이 44,042 버퍼

`MACHINES_SUBQUERY` 는 상관 서브쿼리라 **오락실마다 한 번씩**(939회) 돌았습니다.
그 안에서 `machine_live` · `cabinet_condition` 두 뷰를 건드리는데, 그 뷰들은
`machine_reports` **전체**를 집계합니다. 전체 집계가 939번 재평가된 셈입니다.

```
SubPlan 1
  ->  Aggregate (actual time=0.039..0.039 rows=1 loops=939)
        Buffers: shared hit=43545          ← 전체 44,419 중 98%
```

비교하면 서열표 보드가 77버퍼입니다. **한 요청이 나머지를 다 합친 것보다 무거웠습니다.**

### 고친 방법 — 집계를 한 번만, 그리고 필요한 만큼만

상관 서브쿼리를 CTE 두 개(`cab_agg` · `mach_agg`)로 바꿨습니다. 다만 그냥 바꾸면
**결과가 적을 때 손해**입니다 — 반경 검색(30곳)에서 기체 5,644개를 다 집계하니까요.
처음 그렇게 넣었다가 **반경 검색이 8ms → 26ms 로 3배 느려졌습니다.**

원인은 CTE 가 아니라 **필터 위치**였습니다. 반경 조건이 맨 바깥 `WHERE` 에 있어서
`base` 는 여전히 939곳이었고, 집계를 좁힐 근거가 없었습니다. 필터를 `base` 안으로
당기고, 집계를 `scope`(= `base` 에 남은 오락실)로 좁혔습니다.

목록과 단일 조회가 **한 정의**를 씁니다(`machinesCte(scope)`). 나누지 않은 이유는
이 프로젝트가 이미 겪은 함정입니다 — 같은 규칙을 두 곳에 적으면 한쪽만 고쳐지고,
그러면 목록과 상세가 다른 오락실을 보여 줍니다.

### 측정 — 쿼리 수준 (노이즈 없음)

| 제보 행수 | 기존 버퍼 | CTE 버퍼 | 기존 | CTE |
|---:|---:|---:|---:|---:|
| 25,619 | 44,042 | **163** | 36.0ms | **20.0ms** |
| 42,396 | 42,836 | **162** | 34.6ms | **21.3ms** |
| 65,072 | 48,480 | **163** | 36.1ms | **22.0ms** |

**CTE 는 제보가 2.5배로 늘어도 163버퍼에 고정**입니다. 기존은 48,480까지 늘고,
비용이 `오락실 × 제보` 로 붙습니다 — 제보는 (대기를 뺀 나머지가) 영구 보존이라
계속 늘어나는 축입니다.

### 측정 — 엔드포인트 수준 (여기서는 작습니다)

| 요청 | 기존 | CTE | |
|---|---:|---:|---|
| `/api/arcades` (939곳) | 60ms | **51ms** | **−15%** |
| `?radius=50` (383곳) | 31.5ms | **26.9ms** | −15% |
| `?radius=5` (30곳) | 7.3ms | 10.5ms | +44% |
| `?machines=1` (3곳) | 7.4ms | 9.4ms | +28% |
| `/api/arcades/:id` (1곳) | 5.5ms | 6.3ms | +14% |

**작은 결과에서는 느려집니다.** CTE 자재화와 `IN (SELECT id FROM base)` 준조인의
고정 비용이 남기 때문입니다. 그래도 이 쪽을 골랐습니다.

- 절대값이 **−9ms 대 +1~3ms** 입니다. 그리고 세션당 전체 목록은 1회 나가는 **가장 무거운
  호출**이고(capacity-model.mjs 의 실측 여정), 필터는 대개 클라이언트가 `fullListRef` 로 처리합니다.
- 버퍼가 270분의 1이고 **제보가 늘어도 고정**입니다. 지금은 공짜(shared buffer 적중)라
  지연으로 잘 안 드러나지만, 커지는 축을 끊어 둔 것이 이 변경의 값입니다.
- 정의를 하나로 유지하려면 단일 조회도 같은 모양을 써야 합니다. +0.8ms 로 분기 하나를 없앱니다.

> **엔드포인트 시간으로 이걸 재려 하지 마세요.** `/api/arcades` 는 JSON 직렬화가
> 지배해서 반복 측정 편차가 ±30% 입니다 — 제보를 2.5배로 늘려 가며 재 봤지만
> 노이즈가 효과를 완전히 덮었습니다. 이 변경은 `EXPLAIN (ANALYZE, BUFFERS)` 로 재야
> 보입니다.

### 그런데 부하를 걸면 훨씬 큽니다 — 단일 요청 측정이 과소평가였습니다

위 엔드포인트 표는 **요청을 하나씩 순차로** 잰 값입니다. 목표의 10배 부하
(성장 시나리오 · 2프로세스 · 풀 10 · 스케일 DB)로 다시 재니 이렇게 나옵니다.

| 성장 ×10 (25.4 req/s) | 15절 (CTE 전) | **18절 (CTE 후)** | |
|---|---:|---:|---|
| 처리량 | 25.4/s | 25.4/s | 같음 |
| 중앙값 | 4.2ms | 4.2ms | 같음 |
| p95 | 98.9ms | **51.8ms** | **−48%** |
| p99 | 114.9ms | **67.3ms** | −41% |
| `/api/arcades` p95 | 130.0ms | **70.1ms** | **−46%** |
| 실패 | 0 | 0 | |

**−15% 가 아니라 −48% 입니다.** 이유는 분명합니다 — 44,042버퍼는 shared buffer
적중이라 **혼자 돌 때는 거의 공짜**지만, 동시에 20세션이 돌면 그 메모리 대역폭과
CPU 를 서로 빼앗습니다. 순차 측정에는 경합이 없어서 안 보였습니다.

> **교훈**: 버퍼를 줄이는 최적화는 **부하 아래에서 재야** 합니다. 단일 요청으로는
> "별 차이 없다" 로 읽히고, 실제로 그렇게 읽고 넘길 뻔했습니다.
> (2부 5절이 "왕복이 곧 슬롯 점유" 라고 한 것과 같은 구조입니다 — 자원 경합은
>  한가할 때 안 보입니다.)

### 덤으로 나온 것 — 정렬이 결정적이지 않았습니다

CTE 로 바꾸니 전체 목록의 **순서가 달라졌습니다**(내용은 동일). 원인은 제 변경이 아니라
원래 있던 결함입니다 — `ORDER BY distance_km, name` 에 타이브레이커가 없는데
**같은 이름을 쓰는 오락실이 267곳(66종류)** 있습니다(88오락실 ×2 · 게임랜드 ×4 …).

동명 사이의 순서가 실행계획에 따라 달라지므로, 사이드바가 10곳씩 페이지를 나누는
동안 **같은 곳이 두 페이지에 나오거나 사라질 수** 있었습니다.
`ORDER BY … , b.id ASC` 를 붙여 못 박았습니다.

### 검증

- **17개 필터 조합**(전체·반경 2종·기종 3종·검색 3종·복합·단일 3종·404)에서
  응답이 **바이트 단위 동일**. 전체 목록만 순서가 달랐고, 타이브레이커를 넣은 뒤
  `id` 정렬 기준으로 **내용 동일**·5회 반복 **해시 동일**·DB 의 `ORDER BY name, id` 와 **일치**.
- `npm run typecheck` · 테스트 591건 통과.

# 5부 — 갈라져 있던 가지에서 잰 것 (2026-09-10, 2026-09-15 에 합침)

> 날짜는 4부보다 앞서지만 뒤에 둡니다. 다른 가지(mfa-jwt)에서 따로 재던 것이고,
> 4부를 가리키는 참조가 코드 네 곳에 있어 번호를 밀면 그쪽이 깨집니다.
> 절 번호도 4부 다음(19·20)으로 옮겼습니다 — 원래는 둘 다 10·11 이었습니다.

2부 6번에서 8프로세스를 이미 재 뒀습니다. 이번에 다시 한 이유는 셋입니다.

1. **재현 도구가 저장소에 없었습니다.** 2부는 "k6 사본" 으로 쟀다고만 적혀 있고 그
   사본이 커밋되지 않아, 다시 재려면 매번 새로 만들어야 했습니다.
2. **실사용 구간(20 VU)의 프로세스 A/B 가 없었습니다.** 2부는 200 VU 만 쟀는데,
   임계치를 논하는 구간은 20 VU 입니다.
3. **1 → 2 → 4 → 8 곡선이 없었습니다.** "8이 1보다 낫다" 와 "몇 개가 적당한가" 는
   다른 질문입니다.

측정 도구를 [`load-test/k6-read-multiport.js`](load-test/k6-read-multiport.js) 로
커밋했습니다. 2부가 경고한 `__VU` 함정(init 문맥에서 0이라 포트가 `undefined` 가 된다)의
회피가 코드와 주석에 함께 들어 있고, think time 을 뺀 포화 시나리오(`sat50`·`sat200`·
`sat400`)를 갖습니다.

---

## 19. 프로세스 1 → 2 → 4 → 8

프로세스당 `PG_POOL_MAX` 는 총합이 `max_connections`(100) 아래에 남도록 나눴습니다.
앞단 프록시는 2부와 같은 이유로 두지 않았습니다 — Node 로 만들면 재려는 천장이
프록시로 옮겨옵니다.

### 실사용 구간 — 20 VU · think 1s · 4.5분

| 지표 | 1 프로세스 (풀 30) | 8 프로세스 (풀 11) | |
|---|---|---|---|
| 처리량 | 96.28/s | 98.13/s | +1.9% |
| 중앙값 | 12.07ms | **9.68ms** | −19.8% |
| p90 | 70.15ms | **13.58ms** | **−80.6%** |
| **p95** | 84.84ms | **15.40ms** | **−81.9%** |
| p99 | 105.05ms | **21.12ms** | −79.9% |
| 실패 | 0% | 0% | |

**중앙값은 20% 만 좋아지는데 p90·p95 는 5분의 1 이 됩니다.** 단일 프로세스에서
요청이 이벤트 루프 차례를 기다리며 생기던 꼬리가 사라지는 것이라, 중앙값만 보면 이
개선이 거의 보이지 않습니다. 처리량이 제자리인 것은 시나리오가 매 반복 1초를 쉬기
때문이고(1부와 같은 이유), 이 구간에서 프로세스를 늘리는 값은 **처리량이 아니라 꼬리**입니다.

### 포화 구간 — 200 VU · think 없음 · 90초

| 구성 | 풀 합 | 처리량 | 중앙값 | p95 | 실제 실패 |
|---|---|---|---|---|---|
| 1 프로세스 풀 30 | 30 | 773.2/s | 922.36ms | 2.19s | **25.83%** |
| 2 프로세스 풀 30 | 60 | 887.3/s | 766.03ms | 1.48s | 7.73% |
| 4 프로세스 풀 22 | 88 | 1,212.1/s | 537.75ms | 1.02s | **0%** |
| 8 프로세스 풀 11 | 88 | **1,525.8/s** | **434.21ms** | **846.36ms** | **0%** |

처리량 **+97%**, p95 **−61%**, 그리고 **4개부터 실패가 0** 입니다.

- **풀보다 프로세스가 더 벌어 줍니다.** 4×22 와 8×11 은 풀 총합이 똑같이 88 인데
  처리량은 1,212 대 1,526 입니다. 9번의 결론("슬롯을 늘리는 것보다 왕복을 줄이는 쪽이
  싸다")과 같은 방향입니다 — 슬롯은 이미 남습니다.
- **단일 프로세스의 773/s 는 비교 대상이 아닙니다.** 25.83% 가 실패한 값입니다.
  2부는 같은 자리에서 46.3% 였고, 소켓 수락 단계에서 밀리는 것도 같습니다.
- **실패는 `checks_failed` 로 세야 합니다.** k6 의 `http_req_failed` 는 없는 오락실
  id 에 대한 **404 까지 실패로 세기 때문에** 모든 구성에서 6% 남짓으로 나옵니다.
  그 값을 그대로 쓰면 8프로세스도 실패하는 것처럼 보입니다.

2부와 견주면 오늘이 조금 낮습니다(1,745/s · p95 674ms → 1,526/s · 846ms). 방향과
크기는 같고, 차이는 배경 부하로 보입니다.

### 무엇이 풀렸나 — Pulse 분해 (프로세스당)

| 구성 | 풀 대기 p95 | 순수 실행 p95 | 루프 지연 p95 |
|---|---|---|---|
| 1 프로세스 | 1,245.2ms | 107.5ms | 85.8ms |
| 2 프로세스 | 706.0ms | 103.7ms | 117.3ms |
| 4 프로세스 | 454.5ms | 101.1ms | 102.2ms |
| 8 프로세스 | **392.3ms** | 118.8ms | 107.2ms |

순수 실행(100ms 남짓)과 루프 지연(85~117ms)은 거의 그대로인데 **풀을 기다리는 시간만
3분의 1** 이 됩니다. 5번이 만든 분해가 아니면 이 구분이 보이지 않습니다.

> **⚠ 이 값들은 프로세스 1개분입니다.** 8개가 모두 같은 `PULSE_AGENT_ID` 로 보고해서
> Pulse 가 이들을 한 대상으로 섞습니다(`app.db.qpm` 이 1프로세스 38,918 → 8프로세스
> 13,376 으로 *줄어드는* 것이 그 증거입니다 — 합계가 아니라 대표값입니다).
> 프로세스별로 보려면 agent id 를 나눠야 합니다.

### CPU

8프로세스 · 20 VU 에서 **모든 node 프로세스 합산 평균 0.3코어**(22코어의 1.4%),
최대 0.4코어, RSS 합 1.7GB. 6번이 지적한 "1개만 쓰고 있었다" 는 풀렸고, 이제는
프로세스를 더 늘릴 여유가 **CPU 가 아니라 `max_connections`** 에 묶여 있습니다.

---

## 20. dev 서버를 프로덕션으로 착각했습니다

이번 세션 앞부분의 측정 네 개가 틀렸습니다. 원인은 하나입니다 — **`next dev` 로
재고 있었습니다.** `.claude/launch.json` 의 기본 항목이 `npm run dev` 였고, 그걸
프로덕션 서버로 착각했습니다.

1부 "다시 재려면" 이 이미 경고하고 있습니다: *"부하테스트는 **반드시 프로덕션
빌드로** 재세요. `next dev` 는 수치를 크게 왜곡합니다."* 경고를 읽고도 당했습니다.

| 처음 보고한 것 | 실제 |
|---|---|
| 20 VU p95 **2.19s / 2.33s** — 기준선의 56배 | 프로덕션에서 **84.84ms** |
| 처리량 천장 **~100 req/s** | 1프로세스 포화 **773 req/s** |
| 계측(OTel/Pulse)이 천장의 **27%** 를 먹는다 | **노이즈 범위** (아래) |
| `arcades` 통계 미수집이 원인 | 아님 — 해당 쿼리 **0.26ms**, `arcades_lat_lng_idx` bitmap scan 정상 |

세 번째는 함정이 겹친 경우입니다. ON 을 dev 에서, OFF 를 prod 에서 재고 그 차이를
계측 비용이라고 불렀습니다. 프로덕션에서 같은 조건으로 다시 재면:

| 20 VU · 1프로세스 풀 30 | 처리량 | 중앙값 | p95 |
|---|---|---|---|
| 계측 ON | 96.28/s | 12.07ms | 84.84ms |
| 계측 OFF | 94.57/s | 18.31ms | 124.34ms |

**OFF 가 오히려 느립니다** — 즉 이 구간에서 계측 비용은 실행 간 변동보다 작아
**측정되지 않습니다.** 1부의 "최소 두 번씩 재세요" 가 그대로 적용되는 자리입니다.

두 번째(천장 ~100 req/s)에는 함정이 하나 더 겹쳐 있었습니다. 부하를 **Node 단일
프로세스로 만든 프로브(undici `fetch`)** 로 걸었는데, 그 클라이언트 자체가 천장이었습니다.
같은 서버를 k6(Go, 멀티스레드)로 때리면 773 req/s 가 나옵니다.

> **부하 측정의 첫 줄은 "지금 무엇을 재고 있는가" 입니다.** 서버가 프로덕션 빌드인지,
> 부하 생성기가 서버보다 빠른지 — 둘 다 확인하기 전의 숫자는 숫자가 아닙니다.
> 그래서 `.claude/launch.json` 에 **`arcade-finder (prod)`** 항목을 따로 넣어
> 프로덕션 서버를 이름으로 고를 수 있게 했습니다.

---

## 남은 것 (4부)

- **프록시는 여전히 안 재봤습니다.** 2부·3부의 항목이 그대로입니다. 8프로세스 수치는
  클라이언트 분산 기준이고, nginx·caddy 처럼 **멀티스레드** 인 것을 앞에 두고 다시
  재야 합니다.
- **Pulse agent id 를 프로세스별로 나눠야 합니다.** 지금은 8개가 섞여서 프로세스별
  비교가 불가능합니다 (10번 ⚠).
- **`max_connections` 가 다음 벽입니다.** CPU 는 1.4% 만 쓰는데 풀 총합 88 이
  상한이라 프로세스를 더 못 늘립니다. pgBouncer 나 `max_connections` 상향이 그
  다음이고, 같은 비용이면 여전히 **요청당 쿼리 수를 줄이는 쪽**이 낫습니다.
- **기종 데이터가 여전히 비어 있습니다** — `arcade_machines` 4행. 1부가 "채우면 이
  기준선은 무효" 라고 적어 둔 조건이 아직 유효하고, 채우면 4부도 함께 무효입니다.
