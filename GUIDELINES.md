# 지침서 — 이 프로젝트에서 성능·보안·API 를 다루는 법

> 2026-09-11 작성. 근거는 전부 이 저장소의 실측입니다 —
> [PERFORMANCE.md](PERFORMANCE.md) 4부 · [docs/SECURITY.md](docs/SECURITY.md) ·
> [load-test/README.md](load-test/README.md).
>
> 규칙만 적지 않고 **왜 그 규칙인지와 그걸 어기다 무엇을 잃었는지**를 같이 적습니다.
> 근거를 모르는 규칙은 상황이 바뀌면 잘못 적용됩니다.

---

## 0. 이 프로젝트의 크기 — 모든 판단의 출발점

| | 값 |
|---|---:|
| 목표 가입자 · DAU | 10,000 · 3,000 |
| **평시 피크** | **3.4 req/s** |
| 순간 버스트 ×3 | 10.1 req/s |
| 동시 사용자 | 54명 |
| 쓰기 | 1,245건/일 (**0.062 writes/s**) |
| 읽기 : 쓰기 | **54 : 1** |
| 1프로세스가 받아낸 것 | **69.7 req/s** (목표의 20배)에서 실패 0% |

**숫자를 바꾸려면 `load-test/capacity-model.mjs` 의 `ASSUMPTIONS` 만 고치세요.**
k6 시나리오와 문서가 같은 값을 씁니다.

이 표가 왜 맨 앞에 있는가: 이 프로젝트는 **목표 없이 1년간 최적화했습니다.**
"200 VU 에서 p95 90ms" 같은 좋아 보이는 숫자를 쌓았는데, 그 200 VU 가 우리
서비스로 몇 명인지 아무도 몰랐습니다. 결과적으로 8프로세스·풀 88(= 목표의 162배)
구성을 권고하고 있었고, 정작 진짜 병목(응답이 압축되지 않는 것)은 1년 내내
아무도 못 봤습니다 — localhost 에서 재면 바이트가 공짜라서입니다.

---

## 1. 최적화하기 전에

### 1-1. 목표 대비 몇 배인지 먼저 계산한다

```bash
node load-test/capacity-model.mjs
```

지금 재는 부하가 목표의 100배면, 거기서 나온 병목은 **우리 병목이 아닙니다.**

실제로 걸렀던 것들:

| 하려던 것 | 왜 안 했나 |
|---|---|
| `/api/tier` 왕복 2 → 1 | 목표 부하에서 8ms. 여유 100배 |
| 제보 등록 왕복 4~7 → 3 | 피크 쓰기가 0.062/s → 초당 0.4 쿼리 |
| 만료 제보 정리 호출 간격 제한 | 인덱스 넣은 뒤 0.46ms. 프로세스마다 타이머 상태만 생긴다 |
| 풀 상한 조정 | 목표의 10배 부하에서 **열린 커넥션이 4~5개**(상한 30에 도달조차 안 함) |

### 1-2. 빈 DB 에서 잰 값은 쓰지 않는다

크라우드소싱 계층(제보·리뷰·보유 기종)이 비어 있으면 상관 서브쿼리가 전부 빈
결과를 내서 **공짜로 보입니다.** 같은 부하에서 p95 가 22ms 대 152ms 로 갈립니다.

```bash
# 목표 규모 DB 만들기 (개발 DB 에 절대 돌리지 말 것 — 가드가 막습니다)
psql -w "$BASE/postgres" -c "CREATE DATABASE arcade_finder_scale"
pg_restore -w -d "$BASE/arcade_finder_scale" dev.dump
psql -w "$BASE/arcade_finder_scale" -f load-test/seed-scale.sql
```

⚠ **합성 데이터는 착시를 만듭니다.** `posts` 전체 스캔을 보고 검색 인덱스를
의심했는데, 2만 글 제목이 전부 `loadtest post title N` 이라 `q=loadtest` 가 전부
매칭되는 탓이었습니다. 선택적인 검색어로 다시 재니 인덱스를 정상으로 탔습니다.
**시드가 만든 패턴을 실제 병목으로 착각하지 마세요.**

### 1-3. 재는 방법을 먼저 고른다

| 무엇을 재나 | 쓸 계기 | 쓰면 안 되는 것 |
|---|---|---|
| 쿼리 비용 | `EXPLAIN (ANALYZE, BUFFERS)` | 엔드포인트 시간 (JSON 직렬화가 지배, 편차 ±30%) |
| 요청당 왕복 수 | `pg_stat_database.xact_commit` 증가분 | — 단, **재는 동안 다른 트래픽이 없어야** 합니다 |
| 엔드포인트 지연 | k6, 11회 이상 중앙값 | 단발 curl (콜드 컴파일로 259ms 가 나옵니다) |
| 자원 경합 효과 | **부하 아래에서** k6 | 순차 단일 요청 |
| 대역폭 | k6 `data_received` | 추정 |

**실제로 틀렸던 것들** (같은 실수를 반복하지 않기 위해):

- `pg_stat_database` 로 요청별 왕복을 재다 **헬스체크가 섞여** 제보 1건이 10왕복으로
  나왔습니다(실제 4.2). 클러스터가 2초마다 `/api/health` 를 돕니다 → 헬스체크가
  없는 단일 서버로 옮기고 기저를 확인한 뒤 다시 재세요.
- 같은 계기로 두 번 재서 89,178 과 322 가 나왔습니다 — **통계 수집 지연**으로
  요청별 귀속이 불안정합니다. 그 계기는 버리고 `EXPLAIN` 을 쓰세요.
- 버퍼를 270분의 1로 줄인 변경을 **순차 측정으로 −15%** 로 읽었는데, 부하 아래에서는
  **−48%** 였습니다. shared buffer 적중은 혼자 돌 때 거의 공짜지만 20세션이 동시에
  돌면 메모리 대역폭을 서로 빼앗습니다. **자원 경합은 한가할 때 안 보입니다.**

### 1-4. A/B 는 동시에 띄우고 번갈아 잰다

순차로 재면 배경 프로세스 드리프트가 효과를 덮습니다 — 같은 코드가
631 → 463 req/s 로 흘러내려 **반대 결론**이 난 적이 있습니다.

```bash
NEXT_DIST_DIR=.next-before npm run build
node load-test/bench-server.mjs .next-before 3200 10   # BEFORE
node load-test/bench-server.mjs .next       3300 10   # AFTER
```

### 1-5. 되돌릴 수 없는 것을 먼저 재고, 수치는 반드시 다시 확인한다

측정 전 `pg_dump -Fc` 로 백업합니다. 쓰기 부하는 진짜로 씁니다 — 쏠림 시나리오
한 번에 한 오락실에 제보 8,000건이 쌓입니다.

---

## 2. 지금까지 확정된 사실 (재발굴 금지)

측정으로 배제된 것들입니다. **다시 파기 전에 이 목록을 보세요.**

| 아니었던 것 | 근거 |
|---|---|
| 느린 쿼리 | 워밍업 후 전 엔드포인트 5~25ms. 목표 규모에서도 같음 |
| 반경 검색 (haversine 전체 스캔) | 13ms. **PostGIS 이관 불요** |
| 커넥션 풀 상한 | 목표의 10배에서 열린 커넥션 4~5개(활성 0~1) |
| 프로세스 수 (처리량 측면) | 1개가 목표의 20배를 실패 0%로 받음 |
| 서열표 집계 | 0.67ms · 77버퍼. `charts` 에 미리 계산돼 있음 |
| 게시글 검색 | 1.0ms. trigram GIN 이 받아 줌 |
| XSS | 본문이 JSON 트리 + 화이트리스트. `dangerouslySetInnerHTML` 0곳 |
| 업로드 검증 | 매직바이트 + MIME 허용목록 + 타입별 상한 |
| SQL 인젝션 | 전부 파라미터 바인딩 |

**병목이었던 것**: 응답 압축 부재(바이트) · `/api/arcades` 의 상관 서브쿼리(버퍼) ·
만료 제보 정리의 전체 스캔. 셋 다 **localhost 순차 측정으로는 안 보였습니다.**

---

## 3. 운영 구성

```bash
npm run build
npm run start:cluster        # 인스턴스 2개 × PG_POOL_MAX=10, 공개 포트 3000
```

| | 값 | 왜 |
|---|---|---|
| 프로세스 | **2** | 속도가 아니라 **가용성**. 1개가 목표의 20배를 받는다 |
| `PG_POOL_MAX` | **10**/인스턴스 | 실측 사용량 4~5의 2배 여유 |
| `TRUSTED_PROXY_HOPS` | **1** (런처가 자동) | 프록시가 홉을 하나 더한다 |
| Postgres 피크 RAM | ≈460MB | 접속당 23MB × 20 |

**`next start` 를 바꾸지 마세요.** 4부의 모든 수치가 그것으로 잰 것이라,
`node:cluster` + 커스텀 서버로 갈아타면 기준선이 전부 무효가 됩니다.

수백 req/s 에 가까워지면 nginx·caddy 를 앞에 두고 `API_COMPRESSION=off` 로
압축을 넘기세요. 그때 `TRUSTED_PROXY_HOPS` 도 홉 수만큼 올려야 합니다.

### 알고 있는 한계

- **마이그레이션은 PostgreSQL 에도 기동 시 적용됩니다** (2026-09-13 부터 — 그 전에는
  PGlite 경로만 적용하고 PG 는 풀만 만들었습니다. 이 문단이 그때도 "적용된다" 고 적혀
  있어 코드와 어긋났었습니다). `pg_advisory_lock` 으로 한 프로세스씩 들어가므로 두
  인스턴스가 동시에 떠도 충돌하지 않습니다. 배포 전에 `npm run db:migrate` 로 미리
  적용하는 편이 낫습니다 — 첫 인스턴스의 첫 응답이 늦어지지 않습니다 (deploy/README.md).
- **뷰는 기동마다 DROP → CREATE 입니다.** `exec` 이 파일 전체를 한 번의 simple
  query 로 보내 암묵적 트랜잭션이 걸리므로 다른 세션은 **락을 기다릴 뿐 에러를
  보지 않습니다**(failover 테스트 48/48 통과). 잠깐 멈춤이지 장애는 아닙니다.
- **첨부는 로컬 디스크**(`process.cwd()/uploads`)입니다. 지금 2프로세스는 같은
  cwd 를 공유해 괜찮지만, **인스턴스를 다른 장비로 나누는 순간 깨집니다.**

---

## 4. API 를 새로 만들 때

### 4-1. 상태를 바꾸는 요청은 멱등하게

**뒤집지(toggle) 말고 원하는 상태를 받으세요.**

```
✗ POST   /api/posts/:id/like      → 현재 상태를 뒤집는다
✓ PUT    /api/posts/:id/like      → 켠다
✓ DELETE /api/posts/:id/like      → 끈다

누가 누르는지는 어느 쪽에도 없습니다 — 세션 쿠키가 말합니다 (4-3).
```

토글은 두 가지에 깨집니다.

- **재시도**: 모바일에서 응답을 못 받고 재전송하거나 두 번 탭하면 두 번 뒤집혀
  원래대로 돌아갑니다. 그 수가 인기글 정렬의 근거라 조용히 틀린 순위가 됩니다.
- **낡은 상태**: 두 탭이 모두 "추천 안 함" 으로 보고 둘 다 누르면 토글은
  켜짐 → 꺼짐으로 끝납니다. 누른 사람의 뜻과 반대입니다. `PUT` 은 두 번 다
  "추천하고 싶다" 를 이룹니다.

`setClear(cleared)` · `setSpecial(marked)` · `setLike(liked)` 가 모두 이 모양입니다.
새로 만들 때 이 이름과 형태를 따르세요.

**지금 이 규칙을 따르는 라우트 전부** (2026-09-13 기준):

| 엔드포인트 | 켜기 | 끄기 |
|---|---|---|
| `/api/posts/:id/like` | `PUT` | `DELETE ?commentOffset=` |
| `/api/favorites` | `PUT {arcadeId}` | `DELETE ?arcadeId=` |
| `/api/charts/:id/clear` | `PUT` | `DELETE` |
| `/api/charts/:id/special` | `PUT` | `DELETE` |
| `/api/charts/:id/vote` | `PUT {value}` | `DELETE` (투표 취소) |
| `/api/account/sessions` | — | `DELETE` (다른 기기 로그아웃) |

**끄는 쪽에는 본문을 싣지 않습니다** — `DELETE` 의 본문은 중간 장비가 버리는 경우가
있어, 필요한 값은 쿼리스트링으로 받습니다. 켜는 쪽도 값이 없으면 본문이 없어도 됩니다.

> ⚠ 이 표는 **2026-09-13 전체 점검에서 코드가 문서와 어긋난 것을 찾아 맞춘 결과**입니다.
> 그때까지 이 규칙대로였던 것은 `like` 하나뿐이었고 나머지 넷은 `POST` 였습니다. 동작은
> 멱등했지만(본문에 원하는 상태를 실었습니다) **문서를 보고 `PUT` 을 부르면 405** 가
> 났습니다. 라우트를 추가하면 이 표도 같이 늘리세요 — 표가 없으면 같은 어긋남이 돌아옵니다.

### 4-2. 실패 응답은 `lib/api-errors.ts` 로만 만든다

| 상태 | 언제 | 헬퍼 |
|---|---|---|
| 400 | 경로 id 가 잘못됨 | `badId()` |
| 400 | 본문이 JSON 이 아님 | `badJson()` |
| 400 | 스키마 위반 (+`details`) | `invalid(parsed.error)` |
| 401 | 세션 없음 | `needLogin()` |
| 404 | 대상 없음 | `notFound('…')` |
| 409 | 상태 어긋남 | `fail(409, '…')` |
| 500 | 그 밖의 예외 | `handle()` 이 자동으로 |

**핸들러는 `handle()` 로 감싸세요.** 안 감싸면 예외가 **본문 없는 500** 으로 나가고,
클라이언트는 전부 이렇게 쓰기 때문에

```ts
const data = await res.json();          // ← 여기서 던진다
if (!res.ok) setError(data.error ?? '기본 문구');
```

`res.json()` 이 먼저 던져서 **써 둔 기본 문구가 실행되지 않습니다.** 사용자는 이유를
못 봅니다. (실측으로 확인한 동작입니다.)

**500 본문에 원인을 담지 마세요.** 스택·SQL·연결 문자열이 화면으로 새면 정찰
자료입니다. 사용자가 고칠 수 있는 잘못만 구체적으로 말합니다.

### 4-3. 권한의 근거는 세션이다

`playerId` 를 본문·쿼리로 받아 믿으면 **남을 사칭**할 수 있습니다.

```ts
const guard = await requirePlayer(request);    // 로그인 필수
if (!guard.ok) return guard.response;
const { playerId } = guard;

const playerId = await sessionPlayerId(request); // 비로그인 허용 (제보처럼)
```

⚠ **전부 `requirePlayer` 로 덮지 마세요.** 제보는 로그인 없이도 됩니다 — 지나가다
본 것을 알려주는 자리라 가입을 요구하면 제보가 끊깁니다. 익명은 보유 기종 임계값에
세지 않는 것으로 균형을 잡습니다. **기능이 조용히 사라지는 자리**입니다.

**셋 다 `await` 입니다.** 세션 판정이 쿠키만 보지 않고 `players` 한 줄을 함께
보기 때문입니다 — 서명이 맞아도 **세대 번호**(`token_epoch`)가 다르면 로그인이
아닙니다. 그 번호를 올리는 것이 "세션 끊기" 이고, 비밀번호 변경과
`DELETE /api/account/sessions` 가 올립니다.

쿠키에 든 것은 `{누구, 어느 세대}` 뿐입니다. **이름·관리자 여부를 토큰에 넣지
마세요** — 넣는 순간 7일짜리 스냅샷이 되고, 권한을 뗀 계정이 그만큼 살아 있게
됩니다. 지금은 `getSession` 이 매번 DB 의 값을 돌려주므로 그럴 자리가 없습니다.

값을 캐시하고 싶어지면 먼저 이걸 보세요: 몇 초짜리 캐시라도 그동안은 회수가
회수가 아니고, 인스턴스가 둘이라 어느 쪽에 걸리느냐로 답이 달라집니다.

### 4-4. 목록은 상한과 결정적 정렬을 둔다

- **`LIMIT` 없는 목록을 만들지 마세요.** `GET /api/players` 가 인증도 상한도 없이
  전 사용자 닉네임 370KB 를 내보내고 있었습니다(지웠습니다).
- **정렬에 타이브레이커를 넣으세요.** `ORDER BY distance_km, name` 에 타이브레이커가
  없었고 **같은 이름을 쓰는 오락실이 267곳(66종류)** 이라, 동명 사이 순서가 실행계획에
  따라 달라졌습니다. 페이지를 나누면 같은 곳이 두 페이지에 나옵니다. `id` 를 마지막에.
- **깊은 offset 은 비쌉니다** — 글 2만 건에서 offset 0 은 0.12ms, offset 5,000 은
  **20.9ms** 입니다. 수만 건이 되면 커서 방식(`created_at < ?`)으로 옮기세요.

### 4-5. 집계는 한 번만, 그리고 필요한 만큼만

상관 서브쿼리를 목록 SELECT 에 넣으면 **행마다** 돕니다. `/api/arcades` 가 그래서
한 요청에 44,042버퍼를 썼습니다(서열표는 77). CTE 로 한 번 집계하고 조인하세요.

단, **결과가 적을 때는 전체 집계가 손해**입니다. 필터를 집계 **앞으로** 당기고
(`base` CTE), 집계를 거기에 남은 것으로 좁히세요. 이걸 안 해서 반경 검색이
3배 느려진 적이 있습니다.

---

## 5. 보안

자세한 근거는 [docs/SECURITY.md](docs/SECURITY.md). 새 코드를 쓸 때 지킬 것만:

- **시도 제한 키를 클라이언트가 정하게 하지 마세요.** `X-Forwarded-For` 는 위조
  가능합니다. 계정처럼 **표적이 되는 것**으로 세고, IP 는 `TRUSTED_PROXY_HOPS` 가
  있을 때만 체인 **오른쪽에서** 세어 쓰세요. 모르면 `null` — 틀린 키로 세는 것은
  안 세는 것보다 나쁩니다(방어가 있다고 착각하게 만듭니다).
- **잠금 키를 정규화하세요.** 대소문자·공백·NFC 를 접지 않으면 `Admin`/`admin` 으로
  카운터를 초기화할 수 있습니다.
- **세션 쿠키를 localStorage 로 옮기지 마세요.** 지금 HttpOnly 라 스크립트가 못
  읽습니다. JWT 형식이 필요해지면 쿠키를 유지하고 `Bearer` 를 **추가**하세요.
- **권한 회수가 필요한 판정은 DB 를 다시 보세요.** `requireAdmin` 이 쿠키의 `adm` 을
  믿지 않고 요청마다 확인합니다. 일반 사용자 토큰은 아직 회수 수단이 없습니다(7일).

---

## 6. 로컬 운용 함정 (이 장비에서 실제로 당한 것)

- **psql 인자에 한글을 넣지 마세요** — 셸을 거치며 인코딩이 깨집니다. SQL 은
  ASCII 로 쓰고, 한글이 필요하면 파일로 넘기세요. `curl` 쿼리스트링도 같습니다
  (`q=오락실` 이 빈 결과로 나와 한참 헤맸습니다).
- **`psql`·`pg_dump` 는 PATH 에 없습니다** — `C:\Program Files\PostgreSQL\18\bin\`.
  **반드시 `-w`** (프롬프트가 뜨면 세션이 120초 멈춥니다).
- **부하를 재기 전 3000번대 포트에 낡은 서버가 없는지 확인하세요** — 2시간 전
  프로세스가 포트를 물고 있어서 옛 빌드를 측정한 적이 있습니다.
- **k6 는 `Accept-Encoding` 을 기본으로 안 보냅니다** — 그냥 재면 응답 압축이
  발동하지 않고 `data_received` 가 압축 전 크기로 잡힙니다.
- **`db:init`/`db:reset` 은 개발 DB 본체를 파괴합니다.** `.env.local` 이 모든
  워크트리에 복사되므로 **폴더 격리 ≠ DB 격리**입니다. 2026-08-24 에 실제로 한 번
  날렸습니다(오락실 1,444 → 8).
- **새 마이그레이션을 추가하면 dev 서버를 재시작하세요** — `globalThis` 에 캐시된
  DB 핸들 때문에 `runMigrations` 가 다시 돌지 않습니다.
- **새 워크트리에서 `npm test` 를 처음 돌리면 실패합니다.** 테스트는 `.env.local` 을
  읽지 않아 PGlite 로 가는데, 그 디렉터리가 없으면 **vitest 워커들이 동시에 만들다
  서로 깨뜨립니다** (`could not read blocks 0..0 in file "base/5/…"`). 실패하는 파일이
  실행마다 달라져서 코드 탓으로 보입니다. 한 번
  `npx vitest run --no-file-parallelism` 으로 만들어 두면 그 뒤로는 기본 실행도
  정상입니다 (깨진 채로 남았으면 `.pglite` 를 지우고 다시).
- **여러 워크트리가 같은 개발 DB 를 봅니다.** 지금 개발 DB 에는 어느 브랜치에도
  커밋되지 않은 마이그레이션 050~052 가 적용돼 있습니다 — `db:reset` 하면 복구할
  방법이 없습니다.

---

## 7. 개시 전에 남은 것

| | 무엇 | 왜 급한가 |
|---|---|---|
| ✅ | ~~**R1 병합** — `playerId` 를 믿는 라우트 10개~~ | **끝났습니다** (2026-09-11). 스키마에서 `playerId` 를 빼고 라우트 18곳을 세션 기준으로. `tests/session-identity.test.ts` 24개가 지킵니다 |
| 🟡 | 캐시 헤더 | 34개 라우트가 전부 `force-dynamic`. 참조 데이터에 ETag + `max-age` 만 붙여도 왕복이 사라집니다 — **압축보다 효과가 큽니다** |
| 🟡 | 첨부 썸네일 + 스트리밍 | DB 전체가 176MB 인데 첨부는 글 3개분이 34MB. 구간 요청도 파일 전체를 메모리에 올립니다(28.6MB/요청) |
| 🟡 | R11 — `ADMIN_PASSWORD` 없으면 로그인 503 | 가입은 세션을 직접 심어 통과하므로 **로그아웃 뒤에야** 드러납니다 |
| ✅ | ~~토큰 회수 수단~~ | **끝났습니다** (2026-09-13). `players.token_epoch` 를 토큰에 봉하고 요청마다 대조 — 4-3. `tests/session-revoke.test.ts` 10개가 지킵니다 |
| ✅ | ~~마이그레이션 번호 충돌~~ | **끝났습니다** (2026-09-11). 이쪽 둘을 뒤로 물렸습니다 — 050 → `migrate-053-queue-purge-index.sql`, 051 → `migrate-052-login-failures.sql`(저쪽과 같은 이름이라 합쳐질 때 한 파일). 049 다음이 053 인 이유는 파일 머리말에 |

---

## 부록. 판단의 요약 한 줄

> **목표를 먼저 정하고, 목표 규모 데이터로, 부하 아래에서 재라.**
> 셋 중 하나라도 빼면 이 프로젝트가 1년간 그랬듯 **엉뚱한 곳을 고치게 됩니다.**
