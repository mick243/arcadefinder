-- ============================================================
-- 025 · 리믹스 2곡 추가 제거
--
-- 024 에 이어 2곡을 더 뺍니다. 남는 리믹스는 25곡입니다.
--   리믹스 보유 27곡 − 2곡 = 25곡
--
--   BIG to the BANG   (빅뱅 & 맥스)
--   Infinity RMX      (Synthwulf & 맥스)
--
-- 이 둘은 021(본문 텍스트)이 아니라 **019(리믹스 문서의 표 · PHOENIX)** 가 넣은
-- 곡입니다. 되돌리려면 019 의 해당 VALUES 를 다시 INSERT 하세요 —
-- 아티스트가 NULL 이 아니라 위 괄호 안 값이었습니다.
--
-- ─── 확인한 것 ───────────────────────────────────────────
--   · 2곡 모두 songs 에 실재하고 제목이 정확히 일치합니다.
--   · 채보가 붙어 있지 않아 서열표에는 영향이 없습니다.
--
-- 여러 번 실행해도 결과가 같습니다 — 지울 것이 없으면 0행입니다.
-- ============================================================

DELETE FROM songs
WHERE machine_id = 1
  AND title IN (
  'BIG to the BANG',
  'Infinity RMX'
);
