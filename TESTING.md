# 테스트

```bash
npm test              # 1회 실행
npm run test:watch    # 파일 감시
npm run test:coverage # 커버리지 (coverage/index.html)
npm run typecheck     # tsc --noEmit
```

Vitest + v8 커버리지. 브라우저 없이 노드에서 돕니다.

## 커버리지 기준선 — 2026-08-18

성능·접근성 개선을 "전후"로 제시하려면 시작점이 기록돼 있어야 합니다. 이 표가
그 기준점입니다. **숫자 자체보다 어디가 0인지가 중요합니다.**

| 구분 | 값 |
|---|---|
| 구문 | **10.93 %** (290 / 2652) |
| 분기 | 78.18 % (86 / 110) |
| 함수 | 60 % (18 / 30) |
| 테스트 | 52개 / 4파일 |

| 파일 | 구문 | 비고 |
|---|---:|---|
| `lib/validation.ts` | 98.9 % | zod 스키마 · 쿼리스트링 파서 |
| `lib/community-types.ts` | 100 % | 대기 구간 · 시간 표기 |
| `lib/board-content.ts` | 100 % | 본문 이미지 마커 |
| `lib/board-types.ts` | 100 % | 페이지 번호 창 |
| `lib/board.ts` | **0 %** | 494줄 — 게시판 SQL |
| `lib/auth.ts` | **0 %** | 353줄 — 세션 · 비밀번호 |
| `lib/reports.ts` | **0 %** | 337줄 — 제보 · 자동 반영 |
| `lib/tier.ts` | **0 %** | 332줄 — 서열표 집계 |
| `lib/arcades.ts` | **0 %** | 321줄 — 오락실 조회 |
| `lib/db.ts` · `uploads.ts` · `comments.ts` · `reviews.ts` · `pg-errors.ts` | 0 % | |

분기 커버리지(78 %)만 높은 이유: 지금 덮은 모듈이 전부 **조건 분기가 촘촘한 순수
함수**라서입니다. 구문 10.9 % 가 실제 상태입니다.

### 지금 덮은 것 — 순수 함수부터

DB 없이 돌아가고, 잘못되면 **조용히 틀리는** 로직을 골랐습니다.

- **본문 이미지 마커** (`board-content`) — 첨부가 아닌 id 의 마커가 이미지로
  그려지면 남의 업로드를 본문에 끌어올 수 있습니다. 직렬화 왕복이 안정적이지
  않으면 글을 수정할 때마다 본문이 자랍니다 (실제로 났던 버그)
- **쿼리스트링 파서** (`validation`) — 정렬 키가 SQL 문자열에 보간되는 지점이라
  화이트리스트가 뚫리면 주입입니다
- **제보 스키마** — 종류마다 필수 칸이 다르고, 안 맞는 값은 DB CHECK 에 닿기 전에
  잘라내야 합니다
- **페이지 번호 창** (`board-types`) — 양 끝에서 창이 잘리면 버튼 수가 흔들립니다

### 다음에 덮을 것 — 통합 테스트

0 % 인 모듈은 전부 SQL 을 씁니다. **PGlite 덕분에 도커 없이 실제 Postgres 로
통합 테스트를 돌릴 수 있습니다** — 테스트마다 인메모리 인스턴스를 띄우고
`db/*.sql` 을 적용하면 격리된 진짜 DB 가 생깁니다. 우선순위:

1. `lib/reports.ts` — 있어요/없어졌어요 자동 반영 임계값. 익명 제보를 세지 않는지,
   반대 제보 이후만 세는지. 여기가 틀리면 지도가 조용히 바뀝니다
2. `lib/auth.ts` — 세션 서명 위조, 권한 회수 후 쿠키가 통하는지
3. `lib/board.ts` — 소유자 검사(남의 글 수정/삭제), 페이지네이션 경계
4. `lib/tier.ts` — 클리어 게이트, 집계 재계산

### 테스트가 드러낸 것 — 해결됨

- 대기 인원 상한(12)이 "12명" 인지 "12명 이상" 인지 정해져 있지 않았습니다.
  **`12명` 과 `12명+` 를 별도 항목으로 나눴습니다** — 상한값 자체를 "이상" 으로
  쓰면 정확히 12명인 줄을 12명이라고 말할 수단이 없어집니다.
  `WAIT_OVER`(=13)를 센티넬로 저장하고 표시할 때만 '+' 를 붙입니다
